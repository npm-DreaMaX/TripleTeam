# TripleTeam：Verified Adaptive Agentic Software Delivery Runtime 调研与冻结架构

> 文档性质：架构决策稿，可直接冻结为 ARCHITECTURE.md 的基线。
> 调研截止：2026-09-21。
> 范围：基于 Pi Agent Runtime 构建独立、长期可维护的长程软件工程执行与保证系统；multi-agent 是按需执行机制，不是产品品类。
> 非目标：继续修补 CooperBench harness、兼容旧 CooperTeam、实现另一个 agent loop、比较模型能力或做 benchmark 性能实验。

---

## 0. 最终结论

产品不能继续定位成“更可靠的 Multi-Agent Coding System”。这个说法只有实现手段，没有用户结果，而且 Codex、Claude Code、Copilot 与 Devin 都已经提供多会话、并行 Agent、worktree 或长程执行能力。

冻结后的产品定位是：

> **Verified Adaptive Agentic Software Delivery Runtime for Long-Horizon Repository Changes**
> **面向长程、仓库级变更的可验证自适应智能体软件交付运行时**

这个名称只是技术归类，不是购买理由。真正的用户痛点是：

> **现有 coding agent 让我更快地写代码，却还不能让我放心地把一个大型变更的交付责任完整交出去。**

当工作从“一轮能完成的小修改”变成跨模块、跨上下文、需要多次调查—实现—验证—返工的变更时，使用者实际上仍在充当人工 control plane：亲自拆任务、开会话、盯进度、把测试失败喂回去、判断 Agent 是否跑偏、合并多个 worktree、重跑集成测试、恢复中断现场，并最终检查“完成”是否只是自然语言声明。Claude Code、Codex 和 Pi 已经是优秀的 coding worker；本产品要消除的是它们上方这份持续监督与交付劳动。

因此面向用户的产品承诺冻结为：

> **把一个原本需要开发者持续照看的仓库级变更交给系统；开发者可以离开。回来时只会得到一条可审查、可合并、带精确证据的 Git 变更，或一份明确说明还缺什么、为什么无法安全继续的 BLOCKED 结果。**

它不是所有 coding 请求的默认入口：

- 十几分钟的小修、强交互式探索、需求尚未成形的原型，直接使用 Claude Code / Codex / Pi 更好，本产品明确不竞争。
- 需要数小时、跨多个子系统、会经历失败与重规划、且“差不多完成”会浪费大量人工复核时间的任务，才进入本产品。

这不是很小的垂直场景。它覆盖现有仓库中的大型功能、系统性缺陷修复、架构重构、框架/API/数据迁移、性能或可靠性治理等一整类 repository-scale change。共同点不是任务名称，而是用户希望从“结对编程”升级到“真正委托交付”。

它解决的不是“如何多开几个 Agent”，而是一个完整、广泛且仍未被主流 coding agent 完整解决的问题：

> 给定一个现有 Git 仓库、目标/Issue/Spec 与验收约束，系统能够在无人持续盯守的情况下跨多个上下文、多个执行尝试和进程崩溃持续工作；遇到发现、失败、冲突或评审意见时能够通信并重新规划；最终只交付两类结果之一：一份由最终 Git tree 上的新鲜证据支持、可安全接入的变更，或一份明确列出未满足义务与阻塞原因的失败报告。

用户购买的不是 Agent 数量，而是三件事：

1. **把大任务交出去**：任务可以持续数小时并跨上下文推进，而不是一轮对话结束。
2. **允许执行路径变化**：Agent 能调查、讨论、质疑、求助和重规划，不被静态工作流绑死。
3. **结果可以相信**：系统不因 Agent 说“完成”而完成；每个结论都落到精确代码版本、验证义务、审查和集成状态。

更准确的工程类比不是“Agent 团队聊天室”，而是：

> **一个面向 AI 软件工程的 Git-native transaction manager。**

Agent 是 speculative executor；Attempt epoch 是 fencing token；worktree 是 snapshot；Candidate 是不可变 write-set；Check/Review 是 validation；integration CAS 是 commit；Operation Journal 与 Reconciler 是 WAL/recovery。动态推理可以提出交易和改写执行计划，但不能绕过提交协议。

因此，后文的权威任务图、Attempt、epoch、worktree、Candidate、exact-tree evidence、review、integration、acceptance 和 recovery 仍然成立；需要补强的是 **run-level acceptance contract、事件驱动的动态重规划、失败反馈闭环、实时消息投递和可交付 Git 输出**。这些补强不推翻原核心，而是让它从“可靠的并行执行骨架”成为完整的软件交付运行时。

如果从零实现，我会选择：

**Authoritative Task Graph + Isolated Execution Actors + Evidence-Gated Integration**

中文可称：

**权威任务图 + 隔离执行体 + 证据门控集成**

它是对“Centralized Truth + Decentralized Execution”的收紧，而不是推翻：

> **Centralized Authority + Decentralized Execution + Evidence-Gated Acceptance**

这里必须改三个词义：

1. 中央保存的是可审计的权威记录，不声称保存关于现实的一切“真相”。
2. Agent 不是长期自治节点，而是围绕某个 Task Attempt 创建、可回收的隔离执行体。
3. 完成不是 Agent 的自然语言声明，而是候选变更经过验证、审查、集成和验收后产生的系统决议。

主要参考选择不是“挑一个项目照抄”，而是按边界明确指定一个主参考：

- **产品协作语义主要参考 Claude Code**：采用 Subagents 的隔离委派，以及 Agent Teams 的动态组队、共享任务可见性、直接消息和按需角色；它是闭源产品，所以只把官方公开行为当事实，内部实现只能标成工程推断。
- **控制平面正确性主要参考 Warren**：任务状态机、调度、隔离工作区、reconcile 与 judge/review 边界最接近本文所需的可审计骨架。
- **可执行实现主要来自 Pi**：所有 Agent turn、session、context、工具、provider、compaction 与 runtime lifecycle 都复用 Pi，不根据 Claude Code 的外部行为另造 runtime。
- **Pi 官方 subagent 示例与 mjakl/pi-subagents**：只借 session 派生、subagent 生命周期和并发执行方式。
- **tintinweb/pi-subagents**：借 delegation UX、async lifecycle、并发队列、resume/steer、严格 worktree 启动和 workflow gate；不把 extension 内部 agent 状态当本项目 task truth。
- **Pi Dynamic Workflows**：借 versioned head、append-only hash-chain journal、writer mutex/run lease、checkpoint/resume、orphan settlement 和 human gate；不采用通用工作流语言作为 coding control plane 核心。
- **Gas Town / Beads**：借 durable task ledger、worktree fleet、巡检与集成角色拆分；不采用人格化角色宇宙和过度分布式运营模型。
- **Claude Code Subagents**：借独立上下文、工具权限、恢复和隔离。
- **Claude Code Agent Teams**：借 shared task list、P2P mailbox、显式 claim/ownership、动态 teammate 和 plan gate 的产品语义；避免固定 lead、状态滞后、三态 task list 充当最终验收事实、同文件覆盖和不可恢复的 in-process teammate。
- **Magentic-One / AutoGen**：只借 stall detection、progress ledger 与 replan trigger，不采用由 LLM 文本维护事实账本。

真正需要我们自己实现的是：

- 权威任务图与状态机
- attempt、execution、epoch fencing 和恢复语义
- workspace/candidate/artifact/check-run 模型
- verification、review、integration、acceptance 门控
- typed mailbox 与 proposal protocol
- reconciler、operation journal、审计事件与全局终验
- Pi runtime 和控制平面之间的稳定边界

绝对交给 Pi 的是：

- agent loop
- provider/model 接入
- streaming
- tool-call loop
- session/history
- context 组装
- compaction
- retry/abort/steer
- Pi 原生资源加载与 runtime events

---

## 1. 调研口径与可信度

本结论优先依据当前源码、架构文档、公开 issue 和示例，而不是产品宣传。

| 对象 | 调研基线 | 在本文中的地位 |
|---|---:|---|
| Pi monorepo | 官方发布包 0.86.1；官方 main 重新核验至 commit 7f06f9c | 底层 agent runtime |
| mjakl/pi-subagent | 3.0.3，commit 8e1b40b | Pi 内 supervisor/subagent 参考 |
| tintinweb/pi-subagents | 0.19.0，commit e955e29 | Pi 团队式编排参考 |
| pi-dynamic-workflows | 3.13.0，commit bd27cf0 | durable workflow 参考 |
| tintinweb/pi-tasks | 0.9，commit 29180d7 | task list 与 completion 反例/参考 |
| pi-muselinn | 0.9.22，commit e9d14da | 多角色协作 UX 参考 |
| Warren | 0.19.2，commit ae4028d | 控制平面正确性主参考 |
| Gas Town | commit 649b832 | durable ledger/worktree fleet 参考 |
| pi-superagents | 1.14.3，commit c09aa1c | 领域角色与 Superpowers workflow 对照 |
| Overstory | 0.11，已归档 | 历史经验，不能作为新依赖 |
| AutoGen / Magentic-One | 当前公开文档 | stall/replan 范式参考 |
| Claude Code | 官方文档当前描述 Agent Teams v2.1.178+，并包含后续版本变更说明 | 产品协作语义主参考，无法审查闭源实现 |

证据分为三层：

- **README/文档声称**：项目公开承诺的能力。
- **源码真实实现**：能从代码路径、状态机、数据库 schema、hook 或测试中确认的行为。
- **架构判断**：本文基于上述事实作出的取舍，不把推断冒充实现事实。

许可证原则：

- Pi 为 MIT。
- 对任何拟直接复制或链接的第三方实现，必须在依赖准入时锁定具体 commit，并重新核对该 commit 的 LICENSE、NOTICE、依赖许可证及代码文件头。
- Claude Code 作为重要的产品行为与协作机制参考；公开事实可直接采用，未公开细节可做有证据的工程推断，但不声称与其源码实现相同。
- Overstory 已归档，只吸收设计经验。
- 设计思想可以借鉴；源码复用必须单独通过许可证审查。本文的架构选择不依赖复制任何第三方源码。

---

## 2. 成熟 coding multi-agent 系统的主要范式

项目名称不是最重要的。真正决定系统性质的是 authority、state、ownership、communication、completion 和 recovery 如何组合。

| 范式 | Authority | State | Ownership | 通信 | 完成判定 | 失败与恢复 |
|---|---|---|---|---|---|---|
| Supervisor / Worker | supervisor | supervisor session 或临时调用树 | supervisor 分派 | 父子消息 | worker 回报，supervisor 接受 | 重试或重新派 worker |
| Hierarchical Delegation | 根/中间 manager | 分层局部状态 | 可递归下放 | 父子为主 | 层层汇总 | 局部重启，易丢全局语义 |
| Shared-State Team | lead 或共享任务表 | team task list + mailbox | claim/assign | P2P + broadcast | teammate/lead 更新状态 | 重派、消息协调 |
| Programmed Workflow | workflow engine | workflow journal | 节点预定义 | 数据边与事件 | 节点条件 | checkpoint/replay/retry |
| Blackboard / Durable Ledger Fleet | ledger 与守护进程 | durable task/issue ledger | claim/lease | ledger + mailbox | 状态协议或角色确认 | reconciliation、巡检、重领 |
| Dynamic Orchestrator | planner/orchestrator | plan、progress ledger | 动态选择 | orchestrator 中转 | planner 判断 | stall detection + replan |
| Peer Swarm | 共识、局部规则或无固定中心 | 分散消息/共享内存 | 自组织 | 高频 P2P | 投票/涌现/任一节点声明 | 冗余和再组织，难审计 |
| Verified Control Plane | 控制平面状态机 | 权威任务图 + 证据账本 | active attempt + epoch | typed mailbox + proposal | 验证、审查、集成、验收门 | execution 恢复、attempt 重试 + reconcile |

### 2.1 Supervisor / Worker

优点是边界简单、适合独立子问题、上下文隔离清晰。根本缺陷是 supervisor 同时承担分解、路由、进度记忆、冲突判断和最终验收，长任务中会成为认知和吞吐瓶颈。worker 的“完成”通常仍是自然语言汇报。

### 2.2 Hierarchical Delegation

它解决单 supervisor 并发扇出问题，但引入递归委派树、跨层语义损失、责任漂移和终止判断困难。对 coding workload 来说，递归层级通常不如显式 task DAG 可审计。

### 2.3 Shared-State Team

成员可直接通信、认领任务、并行协作，适合探索性工作。它的弱点是共享任务表经常同时扮演计划、事实和 UI，状态易滞后；同一 workspace 或同文件修改也会产生隐性冲突。

### 2.4 Programmed Workflow

恢复和重放最好，适合确定性流水线。但软件工程问题无法在运行前完整编码成流程图。若把所有协调都做成 workflow DSL，会把动态推理压进脆弱的脚本节点。

### 2.5 Blackboard / Durable Ledger Fleet

任务、消息和 ownership 持久化，进程可崩溃重启，适合长时运行。这一范式最接近成熟工程系统，但如果 completion 仍由 worker 自报，ledger 只是可靠地保存了未经验证的声明。

### 2.6 Dynamic Orchestrator

可以根据进度重规划，适合未知路径问题。其主要风险是 planner 的文本 ledger 被当成事实、频繁重规划导致任务震荡，以及所有观察都回流到中心模型造成瓶颈。

### 2.7 Peer Swarm

对开放研究、创意探索可能有价值；对代码仓库写入、依赖、审计、恢复和验收不合适。团队规模不大时，它的通信成本与不可预测性通常高于收益。

### 2.8 Verified Control Plane

这是本文选择。LLM 负责提出计划、执行和解释；系统负责权限、状态、ownership、验证、集成与恢复。它既不是所有动作都硬编码，也不是让自然语言声明直接改写系统事实。

---

## 3. Claude Code：Subagents 与 Agent Teams 不是同一种范式

### 3.1 Claude Code Subagents

公开设计显示它属于 **Supervisor / Worker**：

- 主 agent 创建或调用 subagent。
- subagent 有独立 context，可配置 prompt、tools、permissions、model、memory、hooks 与 worktree；general-purpose/custom 可恢复，内置 Explore/Plan 是 one-shot、不返回可恢复 agent ID。
- 典型通信路径仍是 parent → subagent → parent；当前版本中，被命名且获得 `SendMessage` 的 subagent 也可以互相传递消息。
- subagent 的结果回到主会话，由主 agent 继续判断。
- worktree isolation 是可选执行能力；当前文档说明 Bash/PowerShell cwd 会阻止回到主 checkout，Bash 还检查 git redirect/command shape。该保护是 Claude Code 自己实现的产品边界，不能凭外部行为假定 Pi 已经等价实现。
- 当前内置类型是 Explore、Plan、general-purpose，用户和项目可定义任意其他 profile。
- 当前版本允许 subagent 继续派生 subagent，默认最多三层；这扩展了委派树，但仍没有 Agent Teams 的共享 task list，也没有 evidence-gated completion。

因此，“能启动多个独立 agent”不等于 Agent Teams。

与它相似的 Pi 项目：

- Pi 官方 subagent extension
- mjakl/pi-subagents
- tintinweb/pi-subagents 的 delegated/async 子集

参考：[Claude Code subagents](https://code.claude.com/docs/en/sub-agents)、[Pi 官方 subagent 示例](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/examples/extensions/subagent/README.md)。

### 3.2 Claude Code Agent Teams

公开设计更接近 **Lead-mediated Shared-State Team**：

- 一个 lead 创建 team。
- teammates 有独立 context。
- 有共享 task list，任务可 assign/claim。
- teammates 可直接互发消息。
- hooks 可约束 teammate idle 与 task completion。
- teammate 可使用独立 worktree。
- plan approval 可形成执行前门控。
- 当前实现不再要求显式 TeamCreate：启用实验开关后，主会话通过带名字的 Agent 调用形成 teammate；一个 session 只有一个 session-derived team。
- task list 使用 pending / in progress / completed 三态，claim 使用文件锁；team config、task list 与 mailbox 都是本地文件，其中 mailbox 是逐 Agent JSON 文件。
- teammate 的 agent type 并不固定，可以来自任意内置或 user/project/managed subagent definition。

公开限制同样重要：

- experimental。
- in-process teammates 不能在会话恢复后完整恢复。
- task status 可能滞后。
- teammates 有时过早标记完成。
- 同文件并行编辑可能覆盖。
- shutdown 可能慢。
- lead 固定，不支持平滑转移。
- 不支持 nested teams。
- teammate 不能创建 teammate，但 in-process teammate 可以前台调用普通 subagent；这和 nested team 不是同一能力。
- split-pane 等行为依赖终端环境。
- 文档所描述的自动 plan approval 仍是以 agent/lead 工作流为中心，不是外部证据验收。

这些不是边角缺陷，而是说明 Agent Teams 的 shared task list 还不是完整的 authoritative control plane。

参考：[Claude Code Agent Teams](https://code.claude.com/docs/en/agent-teams)。

因此 Claude Code 本身也不支持“系统固定四名成员”这一结论。它支持的是：少量内置 profile、任意自定义 profile、由任务动态决定实例数量。本文采用这一点，而不是复制其三态 task truth 或让 Lead 决定最终完成。

### 3.3 Pi 生态定位

| 项目 | 更接近的范式 | 应如何理解 |
|---|---|---|
| Pi 官方 subagent | Supervisor / Worker | 示例级父子委派，不是团队控制平面 |
| mjakl/pi-subagents | Supervisor / Worker | 工程化 session 派生与并发 |
| tintinweb/pi-subagents | Shared-State Team + Supervisor | 更接近 Agent Teams，但 task truth 与执行生命周期仍未完全分离 |
| tintinweb/pi-tasks | Shared task list | 是任务工具，不是验收系统 |
| pi-hive | Peer/role team | 偏角色协作，不是权威状态机 |
| pi-dynamic-workflows | Programmed Workflow | durable orchestration，不等于 coding acceptance |
| pi-muselinn | Shared-State Team UX | 擅长团队协作体验，不应成为状态权威 |
| Gas Town | Durable Ledger Fleet | 最像长期运行的多 agent 运维系统 |
| Overstory | Blackboard fleet | 有价值但已归档 |
| Warren | Verified Control Plane 雏形 | 最适合作为主骨架参考 |

---

## 4. README、源码与架构判断的明确区分

### 4.1 Pi 本体

**公开声称**

Pi 提供 coding agent CLI、SDK、session、extensions、tools、provider 支持、compaction 与事件。

**源码/文档可确认**

- SDK 可创建 AgentSession，注入 tools、resource loader、session manager，并订阅事件。
- runtime 已处理消息流、tool execution、session persistence、branch/fork、compaction、abort/steer 等基础能力。
- Pi 明确不把 permission prompt 或 container sandbox 当作完整安全边界。

**架构判断**

Pi 应是 worker execution runtime，而不是我们的 task database、scheduler、workspace authority 或 acceptance engine。不要把控制平面塞进一个巨大 Pi extension。

参考：[Pi repository](https://github.com/earendil-works/pi)、[SDK 文档](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/docs/sdk.md)、[权限与容器说明](https://github.com/earendil-works/pi#permissions--containerization)。

### 4.2 Pi Durable

**公开声称**

提供 durable agent 的协议与记录类型。

**源码/文档可确认**

当前核心实现仍偏记录契约和内存实现；Pico/SQLite 的完整 durable engine 在 handoff 文档中仍是待完成方向，不能当作已交付数据库层。

**架构判断**

可以借事件与恢复语义，不能把我们所需的权威存储直接假设为 Pi Durable 已经提供。

参考：[Durable README](https://github.com/earendil-works/pi/blob/main/packages/durable/README.md)、[Pico handoff](https://github.com/earendil-works/pi/blob/main/packages/durable/docs/pico-v5-handoff.md)。

### 4.3 tintinweb/pi-subagents 与 pi-tasks

**公开声称**

支持异步 subagent、团队协作、任务分派和 worktree isolation。

**当前源码可确认**

- 0.19.0 的 `AgentManager` 已经具备有界前后台并发、排队、abort、resume、steer、持久 session、nested ownership 和 lifecycle event；这是成熟的 extension 级 Agent lifecycle，不应重新手写。
- 默认 profile 是 general-purpose、Explore、Plan，同时支持 user/project custom agent、extension tool allowlist、`isolated` 和严格 agent-file 解析。
- 最新启动路径在请求 worktree 且功能开启时已经 **fail loud**：创建失败不会再让 Agent 静默回到主 checkout。源码中的低层 `createWorktree()` 仍返回 `undefined`，但 `AgentManager` 把它提升为启动错误。
- workflow gate 会在 child worktree 清理前、准确地在该 worktree 中运行；这说明项目已经修复过“验证错目录”这一类边界问题。
- issue #187 的历史仍有架构价值：修复依赖在 prompt 中强调不得返回主 checkout。它能减少误操作，但不是 OS 级 containment；本项目不能把 prompt 当安全边界。
- worktree cleanup 会隐式 stage/commit/create branch，并在部分清理错误上 best-effort；这适合交互式插件 UX，但与本项目的 Candidate、Operation、CAS 和 forensic retention 语义不同。
- pi-tasks 把 task-list UX 和 subagent lifecycle 连接起来；它提供任务协作体验，但没有本项目的 candidate/evidence/integration acceptance 模型。

**架构判断**

直接借或通过薄适配器复用 profile discovery、session/lifecycle、并发与 steer 的成熟做法；worktree/candidate/integration 由本项目控制，因为这里承载权威边界。拒绝“agent 停止即 task 完成”。

参考：[AgentManager 源码](https://github.com/tintinweb/pi-subagents/blob/e955e29c51b7a6cce37e1108cd2d6c57a77e151c/src/agent-manager.ts)、[worktree 源码](https://github.com/tintinweb/pi-subagents/blob/e955e29c51b7a6cce37e1108cd2d6c57a77e151c/src/worktree.ts)、[issue #187](https://github.com/tintinweb/pi-subagents/issues/187)、[pi-tasks 源码](https://github.com/tintinweb/pi-tasks/blob/29180d7/src/index.ts)。

### 4.4 Pi Dynamic Workflows

**公开声称**

支持持久化 workflow、异步节点、恢复、人类审批与多 agent 编排。

**当前源码可确认**

- 3.13.0 不是简单的内存 workflow：持久层使用小型 versioned head 加 append-only JSONL change log；只回放 head 已提交的字节。
- 每条日志记录包含 generation、sequence、previous hash，读取时校验 hash chain、长度、序号和最终 head hash。
- 写路径有短期 writer mutex，run 执行有跨进程 lease；持久化支持原子 head、backup recovery、checkpoint、pause/resume、journal prefix replay、interrupted-agent settlement 和延迟结果投递。
- workflow script 运行在受限 VM 中，确定性 call identity 使 prefix replay 成立；Agent 本身仍通过 Pi 运行。
- 这些是成熟且值得直接借鉴的 durability 机制。边界不在“它不成熟”，而在领域不同：workflow node/agent call 成功仍不等于某个 Git tree 已完成 candidate、post-integration checks 与 acceptance decision。

**架构判断**

借它的存储协议、幂等、checkpoint、orphan recovery 和 gate 设计；拒绝把其通用 JavaScript workflow DSL 当成本项目权威 task/attempt/candidate/check/integration 模型。拒绝的原因是领域错位，不是实现质量不足。

参考：[3.13.0 README](https://github.com/QuintinShaw/pi-dynamic-workflows/blob/bd27cf0/README.md)、[run record store](https://github.com/QuintinShaw/pi-dynamic-workflows/blob/bd27cf0/src/run-record-store.ts)、[run persistence](https://github.com/QuintinShaw/pi-dynamic-workflows/blob/bd27cf0/src/run-persistence.ts)。

### 4.5 Gas Town / Beads

**公开声称**

以 durable work ledger、worktrees、Witness、Refinery 等角色运行大量 coding agents。

**源码可确认**

- Beads/ledger 和 worktree fleet 是核心。
- Refinery 有 batch/integration 相关代码；部分旧文档与当前实现存在时间差。
- Witness/Refinery 将运行巡检与集成职责分开。
- Gas City 方向进一步讨论 deterministic workspace protocol。

**架构判断**

这是 durable fleet operations 的重要参考。但它偏 agent 自管理吞吐与运营角色，不能直接替代 artifact/evidence acceptance。借机制，不借完整角色世界观。

参考：[Gas Town architecture](https://github.com/gastownhall/gastown/blob/main/docs/design/architecture.md)、[batch.go](https://github.com/gastownhall/gastown/blob/main/internal/refinery/batch.go)、[manager.go](https://github.com/gastownhall/gastown/blob/main/internal/refinery/manager.go)、[Gas City RFC issue](https://github.com/gastownhall/gascity-packs/issues/209)。

### 4.6 Overstory 与 Warren

**公开事实**

Overstory 已归档，Warren 是其后继方向。

**源码/文档可确认**

Warren 把任务图、scheduler、isolated worktree、worker、judge 与状态转移分开。Judge 以只读方式评估并返回 verdict，不直接偷偷改写 terminal state。

**架构判断**

Warren 是最接近本文主架构的现有项目，但我们仍需补强 evidence identity、integration-head CAS、global acceptance 和 Pi-specific runtime boundary。

参考：[Overstory](https://github.com/jayminwest/overstory)、[Warren](https://github.com/jayminwest/warren)、[Warren architecture](https://github.com/jayminwest/warren/blob/main/docs/architecture.md)、[Judge](https://github.com/jayminwest/warren/blob/main/extensions/judge/README.md)。

### 4.7 Magentic-One

**公开声称与实现方向**

Orchestrator 维护 task ledger/progress ledger，发现 stall 后重规划，并决定终止。

**架构判断**

stall detection 很有价值；但文本 ledger 和 orchestrator 的终止判断不能成为 coding artifact 的唯一事实来源。

参考：[Magentic-One 文档](https://microsoft.github.io/autogen/dev/user-guide/agentchat-user-guide/magentic-one.html)。

---

## 5. 重新评价 Centralized Truth + Decentralized Execution

### 5.1 应保留

**ADOPT**

- 单一权威控制平面。
- 独立 session/context/workspace/execution lifecycle。
- task-centric 而非 agent-centric。
- Agent 只能提出状态变更，不能凭自然语言直接提交事实。
- deterministic orchestration 与 dynamic reasoning 分层共存。
- P2P communication 存在，但不是无约束群聊。

这些判断是合理的，而且正好针对现有系统常见的薄弱点。

### 5.2 必须修改

**ADAPT**

1. 将 Centralized Truth 改成 Centralized Authority。
   数据库能权威记录系统决议，但外部世界可能变化，测试也不是绝对真理。系统应保存“基于什么证据、由什么规则、在什么代码身份上作出何种决议”。

2. Agent 不应拥有 task；active Attempt + epoch 才表示写入 authority。
   长期 agent identity 会把失败、上下文和 ownership 绑定在一起。Task 是稳定意图，Attempt 是一次执行，Execution 是一个进程/session。

3. completion 必须拆成多个事实。
   worker_finished、candidate_submitted、verified、reviewed、integrated、accepted 不能压成一个 completed。

4. 中央状态不等于单个 planner。
   数据、规则和写权限集中；推理与执行可以分散。Lead 没有直接覆写事实的特权。

5. evidence 必须绑定精确代码身份。
   只保存“tests passed”没有意义，必须关联 candidate tree hash、命令规范、环境、exit status 和输出摘要。

### 5.3 过度设计，应删除

**REJECT**

- Agent 民主投票或共识协议。
- 无约束自由聊天 swarm。
- 永久人格、复杂组织结构和 agent 简历。
- 任意深度递归 teams。
- 构造通用 BPMN/工作流编程语言。
- 分布式数据库、Raft 或多控制平面。
- 让 LLM 直接编辑数据库状态。
- 把每个 token/stream chunk 都做成领域事件。
- 自动语义 merge 和多 agent 同时写同一 worktree。

### 5.4 更准确的主范式

不存在比它完全不同且更优的主范式；更好的做法是把它精确定义成：

> **一个单写者、可审计、可恢复的任务控制平面，调度短命且隔离的 Pi 执行体；所有代码结果以不可变候选物提交，并通过绑定精确代码身份的证据、审查与集成门控后才获得完成状态。**

---

## 6. 最终架构

## 6.1 整体架构图

~~~text
                         ┌──────────────────────────────┐
 User / CLI / API ─────▶ │        Control Plane         │
                         │                              │
                         │ Command Kernel               │
                         │ Authoritative Store          │
                         │ Task Graph / Scheduler       │
                         │ Attempt Epoch + Policy       │
                         │ Reconciler / Operation Log   │
                         └───────┬─────────┬────────────┘
                                 │         │
                      commands   │         │ events / proposals
                                 │         │
                ┌────────────────▼─┐   ┌──▼───────────────────┐
                │ Pi Process A     │   │ Pi Process B ...     │
                │ session/context  │   │ session/context      │
                │ tools/agent loop │   │ tools/agent loop     │
                └────────┬─────────┘   └─────────┬────────────┘
                         │                       │
                  isolated worktree       isolated worktree
                         │                       │
                         └──────────┬────────────┘
                                    │ candidate refs
                         ┌──────────▼────────────┐
                         │ Git Objects + Artifact │
                         │ local immutable files  │
                         └──────────┬────────────┘
                                    │
             ┌──────────────────────▼──────────────────────┐
             │ Verification → Review → Integration → Accept │
             │ exact tree       findings   CAS head   gates │
             └──────────────────────┬──────────────────────┘
                                    │
                         authoritative transitions
                                    │
                         ┌──────────▼────────────┐
                         │ Domain Event / Trace   │
                         │ Audit / Projection     │
                         └────────────────────────┘
~~~

部署形态是一个逻辑控制平面、一个 SQLite 权威库、多个可独立崩溃的 Pi 子进程，以及受控的 worktree/artifact 目录。逻辑边界必须清晰，但不微服务化。

---

## 6.2 核心模块与唯一职责

| 模块 | 唯一职责 |
|---|---|
| Command Kernel | 校验命令、权限、版本和状态转换；是唯一领域写入口 |
| Authoritative Store | 原子保存当前权威状态、领域事件和本地 operation intent |
| Task Graph | 表达稳定工作意图、依赖、约束和验收契约 |
| Scheduler / Resource Governor | 选择 ready task，以 active Attempt + epoch 授权并限制本机并发 |
| Pi Process Supervisor | 通过 Pi RPC 启动、恢复、steer、abort session；不判定 task 完成 |
| Workspace Manager | 从固定 snapshot 创建、检查、封存和销毁隔离 worktree |
| Artifact Registry | 登记 Git candidate、content-hashed logs/reports 与 provenance |
| Mailbox | 传递 typed、task-scoped 消息；不直接改 task 状态 |
| Verification Engine | 在精确 candidate tree 和声明环境上执行确定性检查 |
| Review Engine | 生成结构化 findings/verdict；默认只读 |
| Integration Manager | 把已批准 candidate 应用到当前 integration head，并处理过期与冲突 |
| Acceptance Engine | 根据 task contract 和全局 gates 产生 authoritative acceptance |
| Operation Journal | 记录 SQLite 事务之外的 Git/文件系统/进程副作用意图与幂等结果 |
| Reconciler | 对账 DB、operation、进程、session、attempt epoch、worktree、artifact 和 integration head |
| Observability | 记录 domain event、runtime trace、metrics 和因果链 |

禁止职责重叠：

- Scheduler 不验证代码。
- Worker Supervisor 不决定 completion。
- Reviewer 不直接写 terminal state。
- Integrator 不重新解释 task intent。
- Agent 不直接写数据库。
- UI/Lead 不绕过 Command Kernel。

---

## 6.3 Authoritative State 到底是什么

权威状态不是一段对话或一个 markdown task list，而是关系化领域记录：

- Run
- Task
- TaskRevision
- Dependency
- TaskChangeProposal
- Attempt
- Execution
- SessionBinding
- Message
- Candidate
- Artifact
- CheckRun
- Review
- Finding
- Integration
- AcceptanceDecision
- Operation
- DomainEvent

Repository snapshot 由 Run 的 input commit/tree 表达；managed worktree 是可恢复的文件系统资源；Evidence 的确定性部分由 CheckRun + Artifact 表达。它们都仍有严格语义，但没有必要各自增加一个空洞的服务实体。

以下内容明确不是 authoritative：

- Agent 的自然语言“已完成”
- Lead 的聊天总结
- 当前 UI 投影
- 某个 worktree 的未登记文件状态
- session transcript 中的计划
- 单独一条“tests pass”消息
- 进程退出码本身
- git branch 名称
- Agent 自己维护的 todo list

### 存储原则

**ADOPT：SQLite WAL + 单领域写者 + 乐观并发控制。**

每个可竞争实体带 version；每个 Task 的 attempt_epoch 单调递增。Command Kernel 在同一事务中写：

1. 当前状态表；
2. append-only DomainEvent；
3. 若命令将触发外部副作用，则写 Operation intent。

外部副作用由本地 operation executor 幂等执行，再回写 observed state。崩溃后 Reconciler 对账 SQLite、Git ref、worktree、Pi session 与 operation。它不是通用消息 outbox。

**REJECT：纯 event sourcing。**

保留完整事件审计，但当前状态使用规范化表直接表达。这样既可审计，也避免所有读取都依赖事件重放和 projection 正确性。

---

## 6.4 Task Model

Task 是稳定意图，不绑定某个 agent 或 workspace。

建议字段：

| 字段 | 含义 |
|---|---|
| task_id | 稳定 ID |
| run_id | 所属运行 |
| parent_task_id | 分解来源，仅用于 provenance |
| spec_revision_id | 当前冻结规格版本 |
| title / objective | 人类可读目标 |
| scope | 允许影响的 repo/path/component |
| constraints | 明确禁止项、兼容性、策略 |
| acceptance_contract | 必须满足的机器检查与审查条件 |
| required_capabilities | 调度要求，不是 agent 名字 |
| risk_class | 决定验证与审批强度 |
| priority | 调度优先级 |
| dependency edges | requires / consumes |
| status | 权威状态 |
| version | CAS 版本 |
| created_by | user / planner proposal / system |
| provenance | 由哪个 proposal 或 task 产生 |

Task 状态冻结为少而稳定的六态：

~~~text
PROPOSED → READY → ACTIVE → ACCEPTED
               ↘ BLOCKED → READY       explicit retry/resolution
任一尚未终结且 policy 允许的状态 → CANCELLED
~~~

`CANDIDATE_AVAILABLE / VERIFYING / REVIEWING / INTEGRATING / STALE / CONFLICTED` 不放进 Task.status。它们分别由 Candidate、CheckRun、Review、Integration 的状态表达；UI 可以派生 phase，但投影不能成为第二份权威状态。状态转换只能由 Command Kernel 根据明确前置条件执行。

依赖类型保持少而硬：

- **requires**：上游必须 ACCEPTED。
- **consumes**：下游 candidate 必须基于包含指定上游 integration record 的 snapshot。

不要加入模糊的“related to”来影响调度；软关系可以作为标签，不进入正确性逻辑。

---

## 6.5 Agent Model

系统不需要永久化“员工”，需要可配置的执行 profile 和可回收 execution。

### AgentProfile

- profile_id
- purpose/instruction template
- allowed tool policy
- capability tags
- context policy
- resource budget
- verification restrictions
- Pi runtime configuration reference

### Attempt

一次完成 Task 的受控尝试：

- attempt_id
- task_id
- base_snapshot_id
- attempt_epoch（单机 fencing token）
- state
- retry_class
- created_at / deadline
- predecessor_attempt_id
- terminal_reason

### AgentSessionBinding

- attempt_id
- Pi session_id
- session lineage
- resume token/reference
- last durable checkpoint
- context manifest hash

### Execution

一个实际进程：

- execution_id
- attempt_id
- local process identity
- heartbeat
- start/stop reason
- observed exit status

关键不变量：

- Task 可以经历多个 Attempt。
- Attempt 可在恢复时经历多个 Execution。
- 同一 Task 只有一个 active writer Attempt；attempt_epoch 是本地 fencing token。
- Agent session 的存在或退出不改变 Task 的验收事实。
- AgentProfile 不是 ownership。

**REJECT：长期 agent CV、自动形成组织政治、跨 run 的无边界自我记忆。**

长期经验应该进入版本化 policy、skill 或知识资产，不进入一个人格不可审计的私有记忆。

---

## 6.6 Agent Lifecycle

~~~text
Attempt:
CREATED → RUNNING → SUBMITTED
             ├────→ FAILED
             └────→ ABORTED
FAILED/ABORTED → new Attempt, never resurrect old authority

Execution:
REQUESTED → STARTING → LIVE → EXITED
                         ├──────→ LOST
                         └──────→ KILLED
~~~

语义：

- Agent 可通过工具提交 candidate、proposal、message、blocked reason 或验证建议；可验收 CheckRun 只能由受控 Check Runner 记录。
- Agent 说“done”只触发 submission intent；如果没有合法 candidate，Attempt 不能进入 SUBMITTED。
- 进程正常退出但没有 candidate：实现 Attempt 进入 FAILED；控制进程若在边界间崩溃，由 Reconciler 先对账后决定恢复同一 Attempt 或失败它。Task 都不会因此完成。
- 进程丢失：Execution 标 LOST。若原 Task、Attempt epoch、profile hash、context manifest 和 worktree 均仍有效，可在同一 Attempt 下创建新 Execution 并恢复原 Pi session。
- 任一恢复条件失效时，原 Attempt 终结；新 Attempt 增加 epoch，旧进程复活后不能写权威状态。
- pause 是 Resource Governor/daemon 的 dispatch 语义，不伪造成 Attempt 状态。
- retry 创建新的 Attempt，保留前一次完整记录。

---

## 6.7 Scheduler 与 Ownership

Ownership 由 **active_attempt_id + 单调 attempt_epoch** 表达，不由聊天承诺表达。单机版不单独实现分布式 Lease 服务。

### 调度条件

Task 可调度必须满足：

- status 为 READY；
- requires 依赖已 ACCEPTED；
- 所需 snapshot 可用；
- 没有其他 active writer Attempt；
- scope 与并行写策略不冲突；
- 资源与 policy 允许；
- retry budget 未耗尽，或有显式 override decision。

所有改变 Task/Candidate 的写命令必须携带并校验当前 Attempt identity 与 epoch。旧 Agent 即使继续运行，其 candidate 或状态 proposal 也会被拒绝。Message 作为 observation 可以保留，但永不产生隐式状态转换。

### 并行规则

**ADOPT**

- 一个 Task 同时最多一个 authoritative writer Attempt。
- 可并行启动多个只读 scout/reviewer。
- speculative writer 只有在系统明确建立 sibling attempts 时才允许；其结果只是多个候选，不能同时集成。
- path overlap 只是风险信号，不是假装完整的语义冲突检测。
- Integration Manager 串行更新 integration head。

### 动态分解与重规划

Planner 或任意 Agent 可以提交：

- CreateTaskProposal
- AmendTaskSpecProposal
- AddDependencyProposal
- CancelTaskProposal
- ReplanProposal

Proposal 先经过 schema、图无环、scope、权限、版本和当前状态校验，再由 policy 自动接受或交给 operator gate。Agent 不直接改 task graph。

---

## 6.8 Communication Model

采用 **typed, durable, scoped mailbox**，不是自由聊天总线。

消息类型保持有限：

- question
- answer
- observation
- help_request
- proposal_ref
- conflict_notice
- handoff

每条消息必须有：

- message_id
- sender attempt/system/operator
- recipients 或 task scope
- related_task_id
- related_artifact/evidence/proposal refs
- created_at
- delivery state
- optional reply_to
- compact payload

规则：

- 消息内容不是状态事实。
- 广播仅限 run-level incident/operator notice。
- 默认 task-scoped，禁止所有 Agent 自动订阅所有消息。
- 新 Agent 不读取全量聊天；控制平面生成 ContextManifest。
- 消息可引用 artifact，禁止在消息内复制大段日志/patch。
- question 可阻塞 Attempt，但只有结构化 blocked transition 才改变调度状态。
- P2P 允许直接沟通，不要求 Lead 转发；涉及 task/state 变化必须走 proposal。

### ContextManifest

每次启动/恢复 worker，控制平面提供版本化 manifest：

- TaskSpecRevision
- base snapshot
- relevant accepted upstream summaries
- selected artifact/check/review refs
- unresolved findings
- scoped mailbox threads
- tool and workspace policy
- attempt identity / epoch

这解决 context propagation，又不重新实现 Pi 的 context window 与 compaction。控制平面决定“给什么事实”，Pi 决定“如何装入和压缩会话”。

---

## 6.9 Workspace / Worktree Model

### RepositorySnapshot

Run 启动时必须冻结输入身份：

- repository identity
- base commit
- optional dirty patch artifact
- submodule state
- relevant environment/lockfile fingerprint

如果用户工作区有未提交修改：

- 要么封存成 snapshot patch；
- 要么明确拒绝启动。

不能悄悄只记 HEAD 而忽略 dirty state。

### Worktree

- 每个 writer Attempt 一个独立 worktree。
- worktree 从 Attempt.base_snapshot 创建。
- Agent 只获得该 worktree 的写工具。
- repo 外写入由 OS/container sandbox 限制；Pi 自身不承担安全边界。
- Workspace Manager 定期检查 git dir、realpath、mount 和 symlink containment。
- worktree 不是结果；Candidate 才是可审计结果。
- Attempt 终止后 worktree 先 seal，再按 retention policy 回收。

### Candidate

Agent 提交时，Workspace Manager 生成不可变候选身份：

- candidate_id
- task_id / attempt_id
- base snapshot
- tree hash
- commit/patch/bundle refs
- changed paths
- untracked file manifest
- generated artifact refs
- submission note
- producing attempt epoch

### Integration Head

控制平面维护唯一 integration head：

- 每次 integration 都以 expected_head 做 CAS。
- 如果 head 已变化，candidate 进入 STALE，需要重新验证、重放或新 Attempt。
- 不允许 Agent 自行宣称“已 merge”。
- Integration Manager 在干净、受控 workspace 中执行应用与合并。
- 同一时刻只有一个 integration commit transaction。

---

## 6.10 Artifact / Evidence Projection

### Artifact

不可变、内容寻址：

- source patch / git bundle / tree
- build output
- test report
- static analysis report
- coverage report
- generated file
- screenshot
- review report
- log excerpt
- reproduction fixture

字段：

- artifact_id
- media/type
- content hash
- size/location
- producer
- task/attempt/candidate linkage
- retention class
- created_at

### Evidence

Evidence 是“在某个精确代码对象上发生过哪些可审计观察”的领域概念，不单建一张 `evidence` 表。确定性证据由 `CheckRun + stdout/stderr Artifact` 表达；语义审查由 `Review + Finding` 表达；最终采用了哪些证据由 `AcceptanceDecision.evidence_refs` 冻结。

CheckRun 至少保存：

- check_id
- subject kind/id 与精确 tree hash
- verifier/check type 与 version
- argv command specification
- environment fingerprint
- start/end time
- state（PASSED / FAILED / ERROR）与 exit status
- normalized result
- stdout/stderr artifact refs
- provenance

Check kind 可以表达 build、targeted/regression test、lint/typecheck、security/static analysis、reproduction、integration check 与 policy check。Review 不伪装成确定性 CheckRun。

禁止复用规则：

- tree hash 不同，默认不能复用。
- verifier 版本或关键环境变更，不能复用。
- candidate 在 integration rebase 后，受影响证据失效。
- Agent 口述的命令结果不是 Evidence；只有受控 verifier 或经 policy 认可的采集器可产生可验收证据。

---

## 6.11 Verification / Review / Integration / Completion

这是架构的中心。

### Verification

Verification Engine：

1. 取不可变 Candidate；
2. 创建干净验证 workspace；
3. 验证 tree hash；
4. 根据 acceptance contract 运行检查；
5. 生成 Evidence；
6. 不直接宣布 Task 完成。

Agent 可建议验证命令，但 acceptance contract 与 policy 决定哪些检查是必需的。失败分类为：

- product failure
- test failure
- infrastructure failure
- flaky/indeterminate
- policy failure

基础设施故障不能被误判成代码失败。

### Review

Reviewer 默认：

- 只读；
- 看到 spec、diff、必要上下文和 Evidence；
- 输出结构化 Finding；
- 每个 Finding 有 severity、scope、evidence refs、disposition；
- verdict 为 approve / request_changes / abstain / invalid_review。

Reviewer 不直接改变 Task terminal state。高风险 task 需要独立 reviewer；低风险可由 policy 允许 machine-only review，但必须是显式规则。

Finding 状态：

- open
- resolved_by_candidate
- waived_by_authorized_decision
- invalidated
- superseded

### Integration

只有满足 candidate-level gates 才进入 integration：

- candidate 来自有效 Attempt；
- required evidence 全部通过且未过期；
- required review 已批准；
- 没有未处理 blocking finding；
- scope 与 policy 合法；
- expected integration head 匹配，或完成了受控 rebase/revalidation。

集成后在新 integration commit 上运行 integration checks。candidate-level tests 不能自动代表 merge 后仍通过。

### Acceptance

Task ACCEPTED 的必要条件：

- candidate 已集成到当前 run 的祖先链；
- integration evidence 满足 acceptance contract；
- 所有 blocking findings 已解决或被授权豁免；
- dependencies 仍满足；
- artifact/evidence provenance 完整；
- Acceptance Engine 生成不可变 AcceptanceDecision。

Agent 无权直接设置 ACCEPTED。Lead 也无权绕过规则；operator override 必须成为显式、带理由、可审计的 Decision。

### Run Completion

Run 完成不能由“所有 task 显示 completed”推导。必须执行全局 gate：

- task graph 中 required tasks 全部 ACCEPTED；
- 无 dangling active attempt/execution；
- integration head 一致；
- repo-level build/test/policy checks 通过；
- 无 unresolved run-level blocking finding；
- 产出 final manifest：input snapshot、task revisions、accepted candidates、integration commits、evidence set、overrides。

---

## 6.12 Recovery / Retry / Resume

| 故障 | 权威处理 |
|---|---|
| Worker 进程崩溃 | Execution 标 LOST；Reconciler 先确认进程消失，再判断同 Attempt 是否可恢复 |
| Pi session 可恢复 | profile/context/worktree/epoch 均有效时，新 Execution 绑定原 Attempt 与原 Pi session |
| authority 失效 | 新 Attempt 增加 epoch；旧执行所有 Task/Candidate 写请求失效 |
| 控制平面重启 | 从 DB 读取状态，Reconciler 对账进程、session、worktree、operation、head |
| DB 事务后外部动作未执行 | Operation Journal 幂等重放 |
| 外部动作完成但确认未写回 | Reconciler 根据 idempotency key 检测并补记 |
| worktree 损坏 | seal 为 forensic artifact；从 snapshot 创建新 Attempt |
| Candidate 验证失败 | 保存证据；Task 回 READY 或 BLOCKED，创建新 Attempt |
| 验证基础设施失败 | retry verification，不惩罚代码 Attempt |
| integration head 变化 | Candidate STALE；重放并重新验证 |
| merge 冲突 | CONFLICTED；创建专门 resolution Task/Attempt |
| planner 无进展 | stall detector 产生 replan request，不直接重写图 |
| Agent 反复失败 | 按 fingerprint/cause 去重；达到 policy 阈值后 BLOCKED，等待新信息或 operator decision |

Retry 不是一个通用计数器。逻辑上区分：

- execution retry
- attempt retry
- verification retry
- integration retry
- replanning

代码 Attempt 预算与 execution recovery、verification infrastructure error 明确分离；operation 使用独立幂等键和终止原因。单机实现不为每类建立一个通用 retry framework。

Resume 只恢复 context/execution continuity，不恢复已经失效的 authority。session 可恢复，不代表旧 attempt_epoch 自动有效。

---

## 6.13 Deterministic Workflow 与 Dynamic Agent Decision

二者通过 **Proposal / Command / Event** 三层共存。

### 动态层：Agent 可以做什么

- 探索代码
- 制定或修改计划
- 提出 task 分解
- 选择实现方法
- 请求帮助
- 提议验证
- 解释失败
- 生成 candidate
- 提交 Finding disposition 建议

### 确定性层：控制平面负责什么

- schema 校验
- 权限检查
- DAG/依赖不变量
- attempt identity / epoch fencing
- workspace 创建与封存
- candidate identity
- evidence validity
- gate evaluation
- state transitions
- integration CAS
- retry/recovery policy
- audit trail

### 协议

~~~text
Agent reasoning
    ↓
Typed Proposal
    ↓
Deterministic validation + current-state CAS
    ↓
Accepted Command or Rejection
    ↓
Domain Event
    ↓
Updated authoritative state
~~~

复杂语义不能全靠规则，所以允许 policy 标记某类 proposal 需要 human 或 reviewer decision。但无论决策者是谁，结果必须成为结构化 Decision，不能停留在聊天里。

**REJECT：在控制平面中执行任意 Agent 生成的 JavaScript/Python 作为工作流定义。**

需要的确定性流程直接编码成有限状态机与 policy rules；需要的动态性通过 Proposal 接口进入。

---

## 6.14 Observability / Event / Trace

分三层保存，避免混淆。

### Domain Events

长期、稳定、可审计，例如：

- TaskProposed
- TaskReady
- AttemptStarted
- AttemptStarted
- CandidateSubmitted
- EvidenceRecorded
- VerificationFailed
- ReviewApproved
- IntegrationCommitted
- TaskAccepted
- ExecutionLost
- OverrideGranted

每个事件包含：

- event_id
- aggregate_id/type
- aggregate_version
- causation_id
- correlation_id
- actor
- timestamp
- payload schema version

### Runtime Trace

用于诊断：

- Pi session lifecycle
- tool call start/end
- worker heartbeat
- token/latency usage
- process stdout/stderr refs
- compaction/resume events

Runtime trace 不是领域事实，不驱动 task 状态，除非 Reconciler 将观察转换为合法 command。

### Raw Pi Session

由 Pi 自己持久化。控制平面只保存 session binding、必要索引与 manifest hash，不复制实现另一套 transcript/context engine。

Observability 必须能从任何 Task 追到：

~~~text
Task
→ Spec Revision
→ Attempt
→ Attempt Epoch
→ Pi Session / Execution
→ Workspace
→ Candidate
→ Evidence / Findings
→ Integration
→ Acceptance Decision
~~~

---

## 6.15 Pi 在其中具体负责哪一层

Pi 是 **Agent Execution Runtime**。

Pi 负责：

- agent loop
- provider/model API
- message streaming
- tool invocation
- session/history persistence
- resume/fork/branch
- context construction
- compaction
- retry/abort/steer
- Pi skills/extensions/resources
- runtime-level events
- 单 agent 的工具执行体验

集成方式：

- 每个受控进程直接运行 Pi；控制平面使用 Pi 官方 `RpcClient` 控制 prompt、steer、follow-up、abort、state 和 events，不复制 Pi loop。
- 控制平面给受控 Pi 进程：ContextManifest、worktree cwd、profile/tool policy 与 Attempt identity/epoch。
- 产品显式加载的薄 control adapter 只向 Agent 暴露三类工具：
  - `orchestrator_context`：读取 scoped task/peer/mailbox 投影；
  - `orchestrator_send_message`：发送 typed question/answer/observation/help/proposal/handoff；
  - `orchestrator_propose_task_changes`：提交版本化 task-change proposal。
- Candidate 不通过 Agent tool 直接登记：Implementer 退出后由控制平面 seal worktree 并生成不可变 Candidate，避免 Agent 伪造 commit/tree identity。
- 这些 tool 只调用控制平面 API；不能直接改数据库。
- Pi session 文件与我们的 authoritative DB 分开。
- Pi extension 可提供 UI 和工具适配，但系统本身不是“一个 Pi plugin”。

明确边界：

> Pi 决定一次 Agent turn 怎样运行；控制平面决定这次运行是否有权做什么，以及它产生的结果能否改变系统状态。

---

## 6.16 我们自己的 Multi-Agent Control Plane 负责什么

必须自己实现：

1. 领域模型与 schema。
2. Command Kernel 和状态机。
3. Task DAG、spec revision 与 dependency semantics。
4. Attempt、Execution、epoch fencing、Scheduler 和 Local Resource Governor。
5. Pi process supervision、RPC adapter 与 session binding。
6. Workspace/Snapshot/Candidate 管理。
7. Artifact/CheckRun 存储、tree identity 与 provenance。
8. Verification plan 执行与证据有效性。
9. Review/Finding/Decision 模型。
10. Integration head、CAS、staleness 与 conflict handling。
11. Acceptance Engine 与 run-level final gate。
12. Typed mailbox 和 proposal protocol。
13. Reconciler、operation journal、idempotency 与 crash recovery。
14. Domain event、audit、trace correlation。
15. Operator CLI/API 和只读 projections。

这些正是现有 Pi runtime 不应负责、现有 multi-agent 插件也没有完整解决的部分。

---

## 6.17 A / B / C 分类

### A. Pi 已经做得很好，绝对不要重造

| 能力 | 决策 | 原因 |
|---|---|---|
| agent loop | ADOPT | Pi 核心职责 |
| provider/model abstraction | ADOPT | 重造只会增加漂移 |
| streaming/tool loop | ADOPT | 已成熟且与 session 紧密耦合 |
| session/history | ADOPT | 控制平面只保存 binding |
| context assembly | ADOPT | 我们只提供 ContextManifest |
| compaction | ADOPT | 不实现第二套摘要引擎 |
| retry/abort/steer | ADOPT | runtime 级行为交给 Pi |
| Pi extensions/skills/resources | ADOPT | 作为 worker 能力 |
| runtime events | ADOPT | 映射到 trace，不复制 |

### B. Pi multi-agent 生态已经做得好，应借鉴或局部复用

| 来源 | 借什么 | 决策 |
|---|---|---|
| Pi 官方 subagent | session 派生、父子执行最小接口 | ADAPT |
| mjakl/pi-subagent | agent definition discovery、persistent-session locking | ADOPT（已通过薄 adapter 复用） |
| tintinweb/pi-subagents | async delegation、mailbox、团队 UX | ADAPT |
| pi-dynamic-workflows | journal、恢复、gate、幂等思路 | ADAPT |
| pi-muselinn | 可观察的团队交互界面 | ADAPT |
| pi-tasks | 轻量 task 操作 UX | ADAPT，但拒绝其 completion 耦合 |
| Warren | task/scheduler/worktree/judge 控制平面骨架 | ADAPT，作为主要参考 |
| Gas Town | durable ledger、巡检、integration role | ADAPT |

“复用”优先指协议和设计。是否直接依赖代码必须经过 API 稳定性、维护状态和许可证审查；核心权威状态机不能外包给一个 UI 插件。

### C. 仍值得我们自己设计的核心

- authoritative task graph
- attempt/execution/epoch fencing
- state transition kernel
- candidate/artifact/evidence identity
- verification contract
- review findings
- integration head
- acceptance decision
- reconciliation/operation journal
- typed state proposals
- run-level completion
- Pi/control-plane boundary

---

## 6.18 明确不要做的能力

| 能力 | 决策 | 原因 |
|---|---|---|
| 自己实现 agent loop/provider/session/context/compaction | REJECT | Pi 已覆盖，且会吞噬维护精力 |
| 自由 peer swarm | REJECT | 难以审计、恢复和控制写冲突 |
| Agent 投票决定完成 | REJECT | 共识不等于代码正确 |
| Lead 作为 truth source | REJECT | 会形成瓶颈和单点认知错误 |
| 所有 Agent 共享同一 workspace | REJECT | 冲突与 provenance 不可控 |
| 同一 Task 多 writer 默认竞速 | REJECT | 成本高且集成复杂；只允许显式 speculation |
| 任意递归团队 | REJECT | 生命周期和权限爆炸 |
| 通用 workflow DSL | REJECT | 过度设计；状态机 + proposal 足够 |
| 自动语义 merge | REJECT | 风险大，冲突应成为显式任务 |
| 全量聊天广播 | REJECT | 通信噪音与 context 污染 |
| 永久 agent 人格与私有长期记忆 | REJECT | 不可审计、难版本化 |
| 分布式控制平面 | REJECT | 当前规模无收益；SQLite 单写者更稳 |
| 纯 event sourcing | REJECT | 增加投影和恢复复杂度 |
| 把 worktree 当 artifact store | REJECT | 可变、难寻址、难保留 |
| 把 benchmark 特例写入核心 | REJECT | CooperBench 只是 evaluator adapter |
| 内建模型选择竞技场 | REJECT | 不讨论模型优劣，且不属于控制平面 |
| 自建容器平台 | REJECT | 使用现有 OS/container primitives，仅实现 workspace policy adapter |
| 花哨组织角色模拟 | REJECT | 角色名不能替代明确职责和权限 |

---

## 7. 现有系统真正没有解决好的核心问题

这些结论来自源码边界、公开 issue 和架构对比，不是为了迎合关键词。

### 7.1 Completion 和 reality 混在一起

最普遍的问题不是 agent 不会说“完成”，而是系统缺少独立的 candidate、evidence、integration 和 acceptance 概念。进程结束、task list 勾选、测试口述、merge 完成常被压成一个布尔值。

### 7.2 状态持久化不等于状态可信

durable task list 可以可靠恢复，但仍可能可靠地恢复一个错误状态。需要 transition authority 和 evidence contract，而不只是 SQLite。

### 7.3 Ownership 缺少 fencing

许多系统有 assigned_to 或 claimed 字段，却没有租约过期后的 fencing。旧 agent 复活后仍可能提交结果，造成双写和状态回退。

### 7.4 Worktree isolation 不等于 containment

创建 worktree 只是第一步。工具 cwd、绝对路径、symlink、repo 外写入、共享缓存和 integration 行为都可能越界。Pi 自己也明确不提供完整 sandbox。

### 7.5 证据缺少代码身份

测试结果若不绑定 tree hash、环境和 verifier 版本，就无法知道它验证的是当前候选、rebase 前候选还是另一个 worktree。

### 7.6 Shared task list 易滞后

Agent Teams 与 Pi 团队插件都暴露过 status lag/stuck 类问题。根因通常是 task state 同时由 UI、Agent 消息、进程生命周期和调度器多方写入。

### 7.7 Planner/Lead 成为认知瓶颈

所有消息和状态都经 lead 汇总，会造成 context 压力、延迟和单点误判。解决方法不是去中心化真相，而是将事实写入结构化控制平面，让 lead 只负责提出计划。

### 7.8 Dynamic replanning 容易破坏审计

LLM 修改任务计划很有价值，但如果直接覆盖旧计划，就丢失因果链、已验证假设和 downstream snapshot 语义。必须使用 spec revisions 和 proposals。

### 7.9 Recovery 经常只恢复进程，不恢复正确性

resume session 不等于恢复任务 authority。还需要检查 Attempt identity/epoch、profile hash、context manifest、workspace、head 和 evidence freshness。

### 7.10 集成后的正确性是独立阶段

各分支单独通过测试，不代表合并后通过。许多系统缺少 integration-head 串行化和 post-integration verification。

### 7.11 Communication 缺少范围与类型

自由聊天让上下文快速膨胀，重要决定埋在 transcript 中。消息必须引用结构化对象，状态变化必须走 proposal。

### 7.12 Failure taxonomy 太粗

“失败后重试”会把 provider timeout、worker crash、代码测试失败、flaky verifier、merge conflict 和规格不清混在一起，造成无效循环。恢复策略必须按失败域区分。

---

## 8. 关键机制 ADOPT / ADAPT / REJECT 总表

| 机制 | 决策 | 说明 |
|---|---|---|
| Centralized Authority | ADOPT | 单一状态写入口与审计 |
| Decentralized Execution | ADOPT | 独立 session/context/worktree |
| Evidence-Gated Acceptance | ADOPT | completion 的核心 |
| Task-centric design | ADOPT | Agent 是可替换执行体 |
| Immutable Candidate | ADOPT | 将可变 workspace 与结果分离 |
| Attempt + Execution + Epoch Fencing | ADOPT | ownership 与恢复基础 |
| Typed durable mailbox | ADOPT | 保留 P2P，限制噪音 |
| Proposal-based replanning | ADOPT | 允许动态推理，不放弃不变量 |
| Single integration head | ADOPT | 串行化仓库事实 |
| Global final gate | ADOPT | 避免局部完成冒充整体完成 |
| Claude Subagent isolation | ADAPT | 使用 Pi 实现相同边界 |
| Claude Agent Teams task claim | ADAPT | claim 升级为 active Attempt + epoch，并与验收状态分离 |
| Claude Agent Teams messaging | ADAPT | 改成 scoped typed mailbox |
| Claude plan approval | ADAPT | 变成 policy/human Decision |
| Warren judge | ADAPT | 强化 evidence identity 和 integration gate |
| Gas Town roles | ADAPT | 转译为模块职责，不照搬人格 |
| Workflow journal | ADAPT | 用 local Operation Journal、events、reconciler 落地 |
| Magentic-One stall/replan | ADAPT | stall 仅触发 proposal |
| Agent self-completion | REJECT | 只能提交 candidate |
| Lead final authority | REJECT | Lead 也是 proposal producer |
| Shared mutable workspace | REJECT | writer attempt 独立 worktree |
| Natural-language state | REJECT | transcript 不是数据库 |
| Unbounded P2P swarm | REJECT | 低信噪比、难恢复 |
| Nested teams | REJECT | task DAG 已表达分解 |
| Distributed consensus | REJECT | 与资源规模不匹配 |
| Rebuilding Pi internals | REJECT | 明确 runtime boundary |

---

## 9. 必须冻结的架构不变量

下面这些应写进代码测试和 architecture decision records。只要它们不变，未来增加 UI、策略或 evaluator 不会推翻核心。

1. 所有 authoritative mutation 必须经过 Command Kernel。
2. Agent、Lead、Reviewer、Integrator 都不能直接写数据库状态。
3. Task、Attempt、Execution、Session、Workspace 是不同实体。
4. Agent exit 永远不等于 Task accepted。
5. 一个 Task 同时最多一个 active authoritative writer Attempt。
6. Task/Candidate 写命令必须校验 Attempt identity 与单调 epoch fencing token。
7. Candidate 不可变并绑定 base snapshot 与 tree hash。
8. Evidence 必须绑定被验证的精确代码身份和 verifier identity。
9. Review 产生 Finding/Decision，不直接产生 terminal state。
10. Integration head 只能由 Integration Manager 通过 CAS 更新。
11. Task ACCEPTED 必须引用 integration record、有效证据和 acceptance decision。
12. Resume 不恢复已失效的 Attempt authority；只有 identity、epoch、profile、manifest 和 worktree 全部匹配才恢复同一 Attempt。
13. Dynamic replanning 只能通过版本化 Proposal 改图。
14. Pi session 是 runtime 记录，不是 authoritative task state。
15. Run completion 必须经过全局 gate，而不是统计 task checkbox。
16. 外部副作用必须可幂等重放或被 Reconciler 检测。
17. CooperBench 通过 adapter 接入，不能污染核心领域模型。
18. 所有 override 必须显式、带理由、带 actor、可审计。

---

## 10. 最终选择

如果现在开始实现，并希望核心以后基本不用推翻：

- **主范式**：Authoritative Task Graph + Isolated Execution Actors + Evidence-Gated Integration。
- **产品协作语义主参考**：Claude Code，但不猜成“源码事实”；借动态 teammate、独立 context、共享任务可见性、P2P 消息和 plan gate。
- **控制平面正确性主参考**：Warren，但不是 fork；借 task/scheduler/worktree/judge/reconcile 骨架，并补齐 evidence identity、epoch fencing、integration-head 与全局验收。
- **Pi 生态补充参考**：
  - Pi 官方：独立 Pi process、并行/chain 调用和四个经过使用的默认职能模板；
  - mjakl：profile discovery、persistent-session lock；
  - tintinweb：async delegation、并发 lifecycle、resume/steer、team UX、严格 worktree 启动和 worktree gate；
  - Dynamic Workflows：versioned journal、恢复、checkpoint、orphan settlement 和 gate；
  - MuseLinn：协作可视化；
  - Pi Tasks：只借交互，不借 completion 语义。
- **Claude Code 应借**：
  - subagent 独立上下文与工具权限；
  - Agent Teams 的 shared task visibility、P2P messaging、claim 和 plan gate。
- **Claude Code 应避免**：
  - lead 成为事实中心；
  - teammate 自报完成；
  - 共享状态滞后；
  - 固定 lead；
  - 同文件无保护并行编辑；
  - session/team 恢复不完整；
  - 自动批准代替外部验收。
- **自己实现**：
  - task/attempt/epoch/state kernel；
  - workspace/candidate/artifact/evidence；
  - verification/review/integration/acceptance；
  - mailbox/proposal；
  - operation journal/reconciler/audit。
- **绝对交给 Pi**：
  - agent loop、provider、streaming、tools、session/history、context、compaction、runtime retry/abort/steer。
- **看起来高级但不要做**：
  - swarm、投票共识、递归团队、通用 workflow DSL、永久 agent 人格、分布式控制平面、自动语义 merge、自建第二套 agent runtime。

- **子 Agent 数量**：内置 profile template 冻结为 4 个——Explorer、Planner、Implementer、Reviewer；实际实例不是 4 个，而是每个 Run 按 task/proposal/policy 产生 **0…N** 个短命 Attempt/Execution，本机并发上限由 Resource Governor 决定。

这套架构保留了 Claude Code Agent Teams 最有价值的独立执行与协作能力，但把它最薄弱的“共享任务表 + Agent 自报状态”替换成了可验证、可恢复、可审计的控制平面。它不是 CooperBench patch，也不是简单 Pi plugin；Pi 是稳定执行内核，而我们构建的是独立的 long-horizon agentic software engineering runtime，multi-agent 只是其中一种按需执行形态。

---

## 11. 面向个人开发者的单机冻结架构

产品目标明确为：运行在单台开发者机器上，解决真实代码仓库问题，并能被普通个人开发者安装、启动、暂停、恢复和检查。

因此，前文的领域正确性保持不变，部署形态收敛为：

> **Local-first Verified Agent Orchestrator**
>
> 一个本地控制进程、一个 SQLite 权威库、多个隔离 Pi Agent 子进程、Git worktree 与本地证据目录。

~~~text
CLI / TUI / Desktop UI
          │
          ▼
┌────────────────────────────────────────┐
│ Local Control Daemon                   │
│                                        │
│ Command / State Kernel                 │
│ Task Graph + Scheduler                 │
│ Local Resource Governor                │
│ Mailbox + Proposal                     │
│ Verification / Review / Acceptance     │
│ Integration Queue                      │
│ Operation Journal + Reconciler         │
│                                        │
│ SQLite WAL                             │
└────────────────┬───────────────────────┘
                 │ local IPC / child process
        ┌────────┴────────┐
        ▼                 ▼
 Pi Agent Process    Pi Agent Process
 session + tools     session + tools
 isolated worktree  isolated worktree
        │                 │
        └────────┬────────┘
                 ▼
       immutable Git candidates
                 │
                 ▼
       local Check / Review Runner
                 │
                 ▼
       serialized Integration Ref
~~~

### 11.1 删除多机部署抽象

**REJECT**

- Worker Host registry
- 服务发现
- 远程调度
- 网络 RPC
- 跨节点 heartbeat
- 跨机器 artifact 传输与复制
- 分布式锁、共识和 leader election

本地 daemon 直接启动和监督 Pi 子进程。多 Agent 并行能力不变，网络与序列化开销消失。

### 11.2 Ownership 冻结为本地 Attempt Epoch

**ADAPT**

不实现独立分布式 Lease 服务。每个 Task 保存：

- active_attempt_id
- attempt_epoch
- worker_pid/process identity
- last_heartbeat

新 Attempt 使 attempt_epoch 单调递增。Agent 提交 candidate、proposal 或状态观察时必须携带 epoch；旧进程即使复活也不能改变权威状态。

保留 fencing 语义，删除分布式租约协议。

### 11.3 Git 作为代码对象存储

**ADOPT**

Candidate 直接表示为 Git commit/tree 和隐藏 ref：

~~~text
refs/tripleteam/runs/<run-id>/attempts/<attempt-id>
refs/tripleteam/runs/<run-id>/integration
~~~

不再为代码实现独立 blob/object service。运行状态不写进用户的目标仓库，而是放在操作系统级本地状态目录：

~~~text
$XDG_STATE_HOME/tripleteam/
  projects/<repository-hash>/
    state.db
    sessions/
    artifacts/
    evidence/
    logs/
    worktrees/
~~~

没有设置 `XDG_STATE_HOME` 时使用 `~/.local/state/tripleteam/`；也允许通过专用环境变量覆盖根目录。目标仓库中只允许出现用户明确选择、应当进入版本控制的项目配置，不保存数据库、会话、日志或临时 worktree。

SQLite 保存路径、content hash、kind 与 provenance。Artifact 领域概念保留，但部署上只是 Git object 或本地内容寻址文件。

### 11.4 Mailbox 收敛为 SQLite Inbox

**ADAPT**

不引入 broker、topic、pub/sub、跨节点确认或广播拓扑。Message 只需：

- id
- sender_attempt_id
- recipient_attempt_id 或 task_id
- kind
- body
- referenced object ids
- created_at
- read_at

消息类型固定为 question、answer、observation、help_request、proposal 和 handoff。消息永远不直接改变 Task 状态。

### 11.5 外部副作用冻结为本地 Operation Journal

**ADAPT**

不构建通用事件总线。保留一张 operations 表，保护 SQLite 与 Git/文件系统之间的崩溃窗口：

- create_worktree
- start_worker
- seal_candidate
- update_integration_ref
- remove_worktree

每个 operation 有 idempotency key、phase、desired state 和 last error。重启后由 Reconciler 补做或确认已完成的本地动作。

### 11.6 删除企业与多租户治理

**REJECT**

- tenant / organization
- RBAC
- service account
- approval quorum
- remote evidence signatures
- artifact replication tiers
- 集群 observability backend

Actor 只需要 user、system、attempt。用户 override 仍需记录原因。工具权限和 sandbox policy 必须保留，因为它们属于本机安全，不属于多租户治理。

### 11.7 Agent Profile 收敛为版本化配置

**ADAPT**

不建立永久 Agent 员工数据库，也不把工作流阶段伪装成四个固定队员。

先区分调研事实：

| 来源 | 源码/文档真实情况 | 架构含义 |
|---|---|---|
| Pi 官方 subagent example | 随源码提供 scout、planner、worker、reviewer，并展示 scout → planner → worker 与 worker → reviewer → worker | 四种职能是经过实际使用的默认值，但示例是 chain/parallel extension，不是权威控制平面 |
| Claude Code Subagents | 内置 Explore、Plan、general-purpose；允许任意 custom definition；按任务自动委派 | profile 集合可扩展，实例按需创建，不是固定团队人数 |
| Claude Code Agent Teams | teammate 数量由任务或用户决定；teammate 可使用任意 built-in/custom subagent type | “team member”与“role template”必须分开 |
| tintinweb/pi-subagents | 默认 general-purpose、Explore、Plan，同时发现用户自定义 profile；支持有界并发、resume/steer 与可配置 nesting | 借 discovery、lifecycle 与有界执行，不把 extension task state 当权威事实 |
| Warren | agent role 是 harness 上的 prompt/policy 配置；judge 是可选观察者 | role 是策略，不是领域实体 |
| pi-superagents | 提供 recon/research/implementation/review/debug 等更细模板 | 证明领域模板有用，但其集合服务于 Superpowers workflow，不是通用本体 |

据此冻结为：

- 系统内置 **4 个默认 profile template**：`explorer`、`planner`、`implementer`、`reviewer`。
- 它们不是四个常驻 Agent，也不是每个 Run 都必须创建四个实例。
- runtime 实例数是 **0…N，按 Task/Proposal/Policy 创建**；单机并发由 Resource Governor 限制。
- Explorer 仅在 Planner 提出结构化 investigation request 时按需创建；默认最多一轮三项，调查失败不会改变 Task truth。
- Planner 是 task-graph proposal producer，不是 Lead truth source。
- Implementer 是唯一默认写 profile，每个 writer Attempt 独占 worktree。
- Reviewer 是独立、只读、按 risk/contract 调用的观察者；确定性 verifier 不是 Agent。
- `PLAN / EXPLORE / IMPLEMENT / REVIEW` 是 Attempt 的 workflow function；`profile_name / profile_version` 是实际 Pi Agent 配置。二者分开存储，不能再混成 role identity。
- 用户级和已信任项目级 Pi agent definition 可以覆盖四个默认模板，也可以增加任意新模板；Task 用 capability/policy 选择 profile，不把 profile 名写进 Task 状态机。
- 项目 profile 只有通过 Pi 官方 project trust 后才加载；受控 worker 默认关闭 ambient extension discovery，只允许产品显式传入的扩展，避免“只读 profile”被环境扩展暗中获得写能力。

取舍：

- **ADOPT**：Pi 的 session/runtime、Pi 官方 `RpcClient`、mjakl 的 profile discovery 与 persistent-session lock。
- **ADAPT**：Pi 官方四职能作为默认模板；Claude 的动态实例化、隔离 context、tool restriction；tintinweb 的有界 lifecycle/steer 思路。
- **REJECT**：固定四人编制、常驻 Agent、role 等于 task owner、无界递归派生、Agent 自报完成、把 verifier/integrator 做成聊天角色。

Attempt 记录 workflow function、profile name、profile version/hash 与 context-manifest hash。Capability 只保留 read、write、review、verify、browser 等实际调度标签。

### 11.8 Task 状态收敛

前文细分的 CANDIDATE_AVAILABLE、VERIFYING、REVIEWING、INTEGRATING、STALE 和 CONFLICTED 不再作为 Task.status。它们属于不同聚合，强塞进 Task 会产生组合状态爆炸。

Task 只保留：

~~~text
PROPOSED
READY
ACTIVE
BLOCKED
ACCEPTED
CANCELLED
~~~

其他状态分别保存：

~~~text
Attempt:
CREATED / RUNNING / SUBMITTED / FAILED / ABORTED

Candidate:
SUBMITTED / REJECTED / ELIGIBLE / INTEGRATED / STALE

CheckRun:
QUEUED / RUNNING / PASSED / FAILED / ERROR

Review:
PENDING / APPROVED / CHANGES_REQUESTED / ABSTAINED

Integration:
QUEUED / APPLYING / COMMITTED / CONFLICTED / FAILED
~~~

UI 根据这些实体派生“正在验证”“等待审查”等阶段，不把投影视图变成第二份事实。

### 11.9 单机必须保留的正确性机制

以下能力不得因“只运行在一台机器”而删除：

1. Task、Attempt、Execution、Pi Session 分离。
2. 每个 writer Attempt 使用独立 worktree。
3. Candidate 冻结为不可变 Git tree/commit。
4. CheckRun/Evidence 绑定精确 tree hash。
5. Agent completion 只能触发 candidate submission。
6. 单一 integration ref 串行更新。
7. 集成后的必要检查。
8. attempt_epoch fencing。
9. daemon 重启后的进程、session、worktree、Git ref 与数据库对账。
10. proposal-based replanning 与 spec revision。

### 11.10 Local Resource Governor

单机产品必须显式管理 CPU、内存、磁盘和子进程，防止多个 Agent 同时运行重型测试导致用户机器不可用。

默认执行通道：

| 通道 | 并发原则 |
|---|---|
| interactive/planning | 1–2 |
| coding agents | 根据 CPU、内存和用户上限动态决定 |
| lightweight checks | 有界并行 |
| heavy build/test | 默认 1 |
| integration | 永远 1 |

Resource Governor 负责：

- 最大 Agent 与 verifier 并发数
- 重型命令互斥
- 进程组创建与清理
- CPU/内存压力反馈
- 前台任务优先级
- pause/resume
- 可选 battery/thermal policy

### 11.11 单机版核心实体

冻结为：

- Run
- Task
- TaskRevision
- Dependency
- TaskChangeProposal
- Attempt
- Execution
- SessionBinding
- Message
- Candidate
- Artifact
- CheckRun
- Review
- Finding
- Integration
- AcceptanceDecision
- Operation
- DomainEvent

降级为字段或配置：

- WorkerHost：删除
- 分布式 Lease：删除；authority 由 Attempt identity + epoch + heartbeat 表达
- RepositorySnapshot：主要由 Git commit/tree 表达
- AgentProfile：版本化配置模板
- Evidence：确定性执行证据主要由 CheckRun 表达
- ArtifactStore：Git objects + 本地文件引用
- 通用 OutboxRecord：删除；本地副作用使用 Operation Journal

---

## 12. Pi 与 Multi-Agent 生态代码复用政策

### 12.1 总原则

> **Reuse first, adapt second, implement last.**

新系统拥有独立控制平面，但不是重新实现 Pi，也不是把成熟 multi-agent extension 换名字后重写一遍。

每个实现任务开始前必须完成：

1. 检查 Pi 当前公开 API 与官方 example。
2. 检查选定 multi-agent extension 是否已有实现。
3. 检查版本、维护状态、测试与许可证。
4. 优先直接依赖稳定 package/export。
5. 无稳定 API 时，使用薄 adapter 隔离第三方变化。
6. 只有控制平面独有领域语义才自行实现。

### 12.2 必须直接复用 Pi 的能力

**ADOPT — 禁止自行重写**

- AgentSession / agent loop
- provider 与 model registry
- streaming
- tool-call execution
- session/history persistence
- session resume/fork/branch
- context loading
- compaction
- abort/steer/follow-up
- runtime retry
- skills/extensions/resources
- Pi runtime events

我们的代码只能配置、调用和观察这些能力。

### 12.3 优先复用 multi-agent extension 的能力

在许可证与 API 允许时，以下能力优先从 Pi 官方 subagent example 或选定的成熟 extension 复用：

- subagent session 创建与派生
- 并发启动与取消
- async result collection
- child session lineage
- Agent 进程生命周期适配
- agent definition/profile loading
- delegation tool schema
- agent output/event forwarding
- session recovery glue
- 已有 worktree adapter 中与本项目不变量一致的路径校验、启动失败处理和 gate 时序

如果 extension 的 task completion、shared-state 或 worktree lifecycle 不符合本文不变量，只替换其控制平面接缝，不重写无关 runtime 部分。worktree helper 只有在能保留 Candidate/Operation/CAS 语义时才直接复用；否则把经过验证的实现经验移植进本项目 Workspace Manager，而 Pi session/runtime 仍原样复用。

### 12.4 必须自行实现的最小范围

只有这些属于我们的差异化核心：

- Task/Attempt/epoch 状态内核
- Task DAG 与 proposal protocol
- SQLite authoritative store
- local operation journal 与 reconciler
- Git snapshot/worktree/candidate/integration 规则
- CheckRun、Review、Finding 与 AcceptanceDecision
- evidence-to-tree identity
- Local Resource Governor
- run-level completion gate

### 12.5 禁止复制后分叉

**REJECT**

- 把第三方 extension 整目录复制进仓库后长期自行维护。
- 修改 Pi 内部实现来承载控制平面。
- 依赖未公开的 Pi 内部文件路径。
- 同时支持多套 agent runtime 抽象。
- 为“以后可能换 harness”设计一套最低公分母接口。

采用 package dependency + pinned version + thin adapter。若确需临时 vendor，必须保留上游 commit、LICENSE、改动清单与回迁计划；它不是默认路径。

### 12.6 复用验收门

任何声称“复用 Pi/extension”的实现必须能回答：

- 直接依赖了哪个 package 与版本？
- 调用了哪些 public exports？
- adapter 为什么存在？
- 哪些代码来自上游，许可证是什么？
- 上游升级时由哪些 contract tests 发现破坏？
- 是否不小心重写了 session、context、retry 或 subagent lifecycle？

这些问题进入代码审查清单和 architecture tests。

### 12.7 已冻结的具体复用选择

以当前实际安装与源码验证为准：

| 能力 | 实现选择 | 决策 |
|---|---|---|
| Agent 进程 RPC、prompt、steer、follow-up、abort、session state 与 events | 直接使用 Pi 0.86.1 公开导出的 RpcClient | ADOPT |
| Pi CLI 与 SDK 定位 | 使用 Pi 公开导出的 getPackageDir 和正式 package entry | ADOPT |
| Agent 定义 discovery | 通过薄 adapter 直接调用 mjakl/pi-subagent 的 agents.ts | ADOPT |
| Persistent Pi session 排他锁 | 通过薄 adapter 直接调用 mjakl/pi-subagent 的 session-lock.ts | ADOPT |
| mjakl 一次性 runAgent | 不作为主 Worker API；它缺少控制平面需要的稳定中途 steer/mailbox handle | REJECT FOR CORE |
| tintinweb AgentManager | 不直接运行；它依赖 Pi ExtensionAPI/ExtensionContext，直接采用会把产品重新塑造成插件 | REJECT FOR CORE |
| tintinweb worktree helper | 最新 `AgentManager` 已把低层 `undefined` 提升为 fail-loud startup，且 workflow gate 在 child worktree 清理前执行；但 helper 的隐式 commit/branch/cleanup 语义不提供本项目需要的 authoritative Candidate、operation replay、integration CAS 与 forensic retention | ADAPT AS REFERENCE，不直接作为核心 workspace runtime |
| Git snapshot/candidate/integration | 直接使用 Git 原生 object、hidden ref、worktree 和 update-ref CAS，由本系统实施严格策略 | ADAPT |
| SQLite capability | 直接使用 Pi 官方 sqlite-node package 导出的 database factory/interface | ADOPT |

mjakl 仓库当前 package.json 声明 3.0.3，但 npm registry 当前只发布到 3.0.1。因此依赖锁定为 Git commit 8e1b40b，而不是复制源码或伪造 npm 版本。该差异由 adapter contract test 覆盖。

此处“拒绝直接采用”不意味着重写其 agent runtime。拒绝的是与 extension host 强耦合或违反本系统不变量的控制逻辑；底层 Pi runtime 仍全部复用。

---

## 13. 单机产品最终冻结结论

部署层采用模块化单体，不采用微服务：

- 一个 Local Control Daemon
- 一个 SQLite WAL 数据库
- 多个受监督的 Pi Agent 子进程
- 每个 writer Attempt 一个 Git worktree
- Git refs 表达 candidate 与 integration identity
- 本地文件保存非代码 artifact、evidence 与 logs

逻辑层仍完整保留 Task、Attempt、Candidate、Check、Review、Integration 与 Acceptance。删除的是分布式和企业治理成本，而不是正确性机制。

项目依赖边界必须始终保持这个方向：

1. Pi 与 multi-agent package 的 public API 通过 pin 和 contract test 锁定。
2. 所有上游变化只能在薄 Pi adapter 内吸收。
3. 独立 control-plane domain kernel 不依赖 extension host context。
4. CooperBench 只在核心稳定后作为 evaluator adapter 接入，不能反向决定领域模型。

此结论替代文档中任何暗示多主机、对象存储服务、broker、RBAC 或通用 workflow engine 的部署设计。

---

## 14. Claude Code 参考方式与实现来源约束

Claude Code Subagents / Agent Teams 是本文的**产品协作语义主参考**，Warren 是**控制平面正确性主参考**，Pi 是**真实可执行 runtime 来源**。三者不是竞争关系：Claude Code 告诉我们广大个人开发者应看到怎样的委派与团队体验，Warren 提供可审查的状态/调度边界，Pi 提供不应重写的 Agent runtime。Claude Code 的公开资料能够直接支持其外部行为和产品语义；对未公开的状态存储、进程模型或调度细节，可以结合公开行为做有证据的工程推断并实现，但必须把“官方公开事实”“工程推断”“本项目决策”分开标注。

新项目明确借鉴：

- 独立 context/session；
- delegation 与 teammate specialization；
- team-visible task/communication UX；
- Lead 可以协调但不是最终 truth source；
- Agent Teams 中并行执行、消息和共享任务视图的用户价值。

**ADAPT**：公开文档已经讲清的协作机制直接进入设计；能够解释其外部行为、且符合本项目不变量的内部机制可以推断实现。

**REJECT**：把推断冒充源码事实，或不检查 Pi 是否已有实现就机械手搓一个所谓“Claude team runtime”。Claude Code 没有公开源码意味着不能直接复用其代码，不意味着不能学习、推断和实现其优秀机制。

实现来源按以下顺序冻结：

1. Agent loop、session、context、tool runtime、provider、compaction、runtime retry、abort/steer/follow-up 和 runtime events：直接复用 Pi harness。
2. Agent profile discovery 与 persistent session locking：直接复用已锁定的 Pi multi-agent package；其 concurrency/result collection 不复用，因为本项目的并发许可、Attempt epoch、Candidate 与验收生命周期必须由控制平面统一裁决。
3. 从 Claude Code 采用或推断的协作机制：映射到本项目自己的 Task、Attempt、Message、Proposal 和 Evidence 协议，但所有 Agent 执行仍由 Pi 驱动。
4. 只有 authoritative task graph、epoch fencing、candidate identity、verification gate、integration CAS、operation journal、reconciliation 和 acceptance decision 由本项目实现。

因此，可以推断 Claude Code 的合理实现并吸收它的优秀机制，但不能把推断说成源码事实；“基于 Pi”也不等于“把 Pi plugin 当作产品宿主”。产品是独立控制平面，执行内核和能安全拆出的 multi-agent 基础能力尽量直接来自 Pi 生态。

---

## 15. 当前实现与冻结架构的对应关系

这不是“未来路线图”，而是当前仓库已经落地的核心边界。文件名只说明职责入口，不改变上文的领域模型。

| 冻结模块 | 当前实现 | 已验证的不变量 |
|---|---|---|
| Authoritative Store | `src/store/database.ts`、`src/store/schema.ts` | SQLite migration、外键、严格状态枚举、v1→v7 数据迁移；冻结 GoalContract；持久化 coordination assessment/contract/decision、failure diagnosis、DecisionRequest、exploration、usage 与 RunReport |
| Command Kernel | `src/control/kernel.ts` | Agent 不能直接 ACCEPT；epoch fencing；Kernel 自读 frozen contract 派生 required checks/review，caller 不能少传 evidence 绕过；DecisionRequest 与 stale Attempt authority 校验 |
| Read Model | `src/control/catalog.ts` | Task、Attempt、coordination、failure、decision、exploration、artifact、event 的只读查询；为后继 Attempt 生成有界 failure/coordination packet |
| Pi Runtime Boundary | `src/runtime/pi/rpc-worker.ts`、`launcher.ts`、`upstream.ts` | 官方 RpcClient；mjakl discovery/session lock；默认关闭 ambient extension discovery；从 Pi events 采集 token/cache/cost/tool-call usage |
| Agent Coordination Adapter | `src/runtime/pi/control-bridge.ts`、`control-extension.ts`、`live-attempts.ts` | 显式 Pi tool extension + attempt-scoped loopback capability；authoritative context 包含 contract/exploration/decision；durable-first message；受限 Human-on-Exception request；Agent 无 DB/accept/integration 权限 |
| Planner / Explorer | `src/planning/plan.ts`、`src/exploration/explorer.ts` | schema 化 task DAG + decomposability/sequentiality/coupling/cost/uncertainty assessment + TaskInterface；按 hypothesis/baseline 去重的有界 diverse exploration |
| Scheduler / Resources | `src/control/scheduler.ts`、`coordination-policy.ts`、`resource-governor.ts`、`task-proposal-policy.ts` | 持久化选择 `SINGLE/PARALLEL_TASKS/SERIALIZE/DIVERSE_EXPLORATION`；scope、semantic/interface coupling、integration cost 与 resource budget 联合门控；material change 形成 DecisionRequest |
| Attempt Execution | `src/control/task-executor.ts`、`failure-policy.ts` | 同一 Attempt 可多 Execution；failure fingerprint 与 `RETRY/REPLAN/DELEGATE/DIVERSE_EXPLORE/REBASE_REVERIFY/INFRA_RETRY/ESCALATE/BLOCK`；相同失败有界；infra check 原地重试 |
| Workspace / Candidate | `src/workspace/git.ts` | dirty/untracked input snapshot；writer worktree；immutable commit/tree；integration ref CAS；正常 Git delivery branch |
| Verification / Artifact | `src/verification/check-runner.ts`、`run-verifier.ts` | argv 无 shell；CheckRun 绑定 tree/check-version/evidence-class；FAILED 与 ERROR 分离；stdout/stderr content hash 与 provenance |
| Review | `src/review/reviewer.ts` | 独立只读 Pi review；结构化 finding；review 不直接完成 Task |
| Operation / Recovery | `src/control/operation-journal.ts`、`reconciler.ts` | 幂等 observed result；live Execution→LOST；可恢复 Attempt 保留；integration intent 对账 |
| Delivery | `src/delivery/reporter.ts` | 终态幂等投影；按最终树证据区分 `VERIFIED_DELIVERY/STRUCTURAL_HANDOFF/BLOCKED/CANCELLED`；final commit/tree、goal contract、evidence provenance 写入不可变 manifest |
| Product Surface | `src/app/orchestrator.ts`、`src/cli`、`src/daemon` | 独立 CLI/daemon；pause/resume/continue；proposal 与 first-class decision 的查看、裁决、自动续跑；terminal result |

51 项核心 contract test 覆盖：Agent submission 不等于 acceptance、stale Attempt 不能提交/请求人类裁决、session/context 不匹配不能 resume、不可执行 proposal 在持久化前拒绝、task capability 保真、受控动态 refinement、semantic coupling 强制串行、解释性 adaptive parallelism、高不确定性先探索、重复 failure fingerprint 换策略且不会无限重试、first-class DecisionRequest、scope ownership、durable/live message、原子 cancellation fencing、post-integration exact-tree evidence、Kernel frozen-contract gate、run delivery classification、operation replay、recoverable Attempt、dead-owner session lock recovery、worktree CAS、daemon token 和上游 adapter。构建交付前固定运行：

~~~bash
npm run check
npm test
npm run build
~~~

明确未伪装为已实现的内容：TUI/desktop UI、OS 级 sandbox、CooperBench evaluator adapter、自动语义冲突解决。前三者不属于核心架构正确性的替代品；自动语义 merge 明确 REJECT。

---

## 16. 产品定位与对外表述冻结

### 16.1 产品类别

项目名冻结为 **TripleTeam**，但不将产品类别定位为 “Multi-Agent Coding System / Multi-Agent Framework”。技术类别是：

> **Long-Horizon Agentic Software Engineering Runtime**
> 面向长程仓库级软件任务的可靠 Agent Runtime / Agent Systems 项目。

Multi-Agent 只是动态执行模式。简单任务可以只创建一个 Implementer；只有存在独立、dependency-ready 且 scope 不冲突的任务时才并行创建多个 Attempt。产品价值不是 Agent 数量，而是长任务能否在失败、中断、重规划和集成变化后仍产生可信交付。

面向个人开发者的核心理由是：Claude Code、Codex 和 Pi 已经能很好地写代码，但长程仓库变更仍经常要求用户自己充当 control plane。本产品接管的是持续监督、失败证据回灌、多会话 ownership、崩溃恢复、worktree 合并、最终复验与交付判定；十几分钟的小改仍应直接使用原生 coding Agent。

### 16.2 英文一句话

> Designed and implemented a verified, recoverable agentic software engineering runtime for long-horizon repository changes, supporting adaptive single-/multi-agent execution, crash recovery, Git-native transactional integration, and evidence-gated delivery.

这句话可直接使用，因为其中每项能力都有当前源码映射；不得擅自附加未执行的 benchmark 数字。

### 16.3 四个可证明的竞争力

1. **Agent Systems control plane**：Task / Attempt / Execution 分离；epoch fencing；versioned task proposal；scope-aware scheduler；Pi session resume；typed durable/live mailbox；Agent 推理与 authoritative transition 分离。
2. **长程可靠性**：daemon/worker 中断后通过 SQLite、operation journal、Git refs、worktree 与 Pi session 对账；同一 Attempt 可在新 Execution 中恢复，失去 epoch 的旧进程无法回写。
3. **Git-native correctness**：输入 snapshot、每 Attempt 独立 worktree、immutable Candidate commit/tree、integration CAS、合并后重新验证、normal delivery ref 与 exact-tree manifest。
4. **Evidence-gated delivery**：Agent 的 Done、进程 exit 0、review prose 都不是完成事实；Task 必须由外部 check/review/integration evidence 驱动 ACCEPTED，Run 必须对 frozen GoalContract 和最终 tree 完成 fresh gate。

### 16.4 可以声称的 harness 层专项工程

本项目没有 fork 或重写 Pi harness，但确实实现了面向长程 Agent 的 runtime adaptation layer：

- 用 Pi 官方 RPC 启动和控制子进程，同时将 Pi session identity 绑定到 Attempt，将每次进程 incarnation 绑定到 Execution；
- 以 context manifest hash 固定首次 assignment，拒绝用变化后的 authoritative context 偷换恢复中的 session；
- 默认关闭 ambient extension discovery，只显式装载选定 profile tools 与 attempt-scoped control adapter；
- daemon crash 后保留原 Pi session/worktree，以新 Execution 恢复；persistent session lock 只在 upstream guard 确认 owner PID 已死亡时回收；
- mailbox 先写 authoritative store，再通过 Pi `followUp` 最佳努力推入 live session；实时传输失败不丢 durable message，也不伪造状态变化；
- 新 Attempt 获得前序失败原因、check 输出尾部、review findings、integration conflict 与 predecessor lineage，不进行 blind retry；
- Agent task-change tool 不接收 acceptance contract，检查与 review policy 由控制面注入，避免 reasoning layer 降低自己的验收门槛。

准确表述是“在 Pi 公共 harness 能力之上实现长程可靠性适配”，不能说“修改了 Pi 内部 agent loop”或“重新实现了 context/compaction/retry”。

### 16.5 Memory 边界

系统具备与任务可靠性直接相关的**持久执行记忆**：SQLite authoritative state、domain event trail、Pi session history、typed mailbox、content-addressed artifacts、Git object/ref 和 failure packet。它们服务于 crash resume、handoff、审计和 evidence-aware retry。

系统明确不做通用 RAG memory、跨项目用户画像、永久 Agent 人格或自动积累“经验规则”。这些机制既不是当前用户痛点的必要条件，也可能把旧结论和错误经验注入新的 authoritative run。

### 16.6 可用于简历的技术要点

- Built a standalone long-horizon agentic software engineering runtime on Pi RPC, separating LLM reasoning from authoritative Task/Attempt/Execution state transitions through epoch fencing and versioned proposals.
- Implemented crash-safe session/worktree recovery and idempotent operation reconciliation, preserving valid Pi context while preventing stale workers from submitting Candidates or mutating task truth.
- Designed Git-native transactional delivery with isolated worktrees, immutable commit/tree identities, serialized compare-and-swap integration, post-merge re-verification and exact-tree evidence manifests.
- Added adaptive single-/multi-Agent scheduling, durable-to-live typed messaging, bounded autonomous replanning and evidence-aware retries without reimplementing Pi's agent loop, providers, tools, context or compaction.

以上 bullets 只描述当前实现机制。任何外部解题率、稳定性或成本数字只有在真实运行并保存可审查证据后才能追加；“计划测试”不能写成“已经取得结果”。

---

## 17. 最终实现冻结：Adaptive Coordination Layer

本节是源码完成后的最终口径，覆盖并替代前文任何仅以 “scope 不冲突即可并行” 或笼统 `DELIVERABLE` 描述当前实现的段落。

### 17.1 项目最终解决的工业问题

项目解决的不是“怎样启动多个 Agent”，而是：

> 面向复杂代码任务，如何在增加 Agent 计算和并行度时，把计算转化为更高的**可验证工程吞吐**，同时避免任务依赖、重复探索、语义承诺漂移、并发冲突、失败传播和重新集成成本吞掉收益。

个人开发者面对长程仓库任务时，通常要自己判断是否拆分、协调多个上下文、转发失败证据、处理 stale branch、重跑最终检查并决定何时可信交付。本系统把这些模型外部语义做成可恢复的控制平面。小型、单轮、交互式修改仍应直接使用 Claude Code、Codex 或 Pi；本项目只在“长任务 + 多次失败/重规划 + 一个或多个执行体 + 最终需要可信交付”的区域提供结构性价值。

系统优化的是：

$$
Verified\ Engineering\ Throughput =
\frac{\text{最终树上通过所需验收的工程任务量}}
{\text{wall-clock 时间或 developer supervision time}}
$$

实现已经记录支持未来计算该指标的事实：parallel window、Agent token/cache/cost/tool calls、check/review/integration 时间、retry/replan、stale/invalidation、exploration、DecisionRequest 等。仓库当前没有声称任何尚未真实运行的 benchmark 数字。

### 17.2 三个一级贡献

#### Contribution 1 — Adaptive Execution

`AdaptiveCoordinationPolicy` 对每个 ready task 使用 Planner 产生并经 schema 校验的五维 assessment：decomposability、sequentiality、semantic coupling、integration cost、uncertainty；再结合 DAG readiness、scope overlap、TaskInterface、当前 integration head 和本机 resource budget，在四个模式中保守选择：

- `SINGLE`：只有一个 dependency-ready task；
- `PARALLEL_TASKS`：至少两个任务同时满足 path-disjoint、低 sequentiality、低 semantic coupling、非高 integration cost、非高 uncertainty，并且 interface/provides/requires 不形成隐含依赖；
- `SERIALIZE`：缺少充分独立性证据，或存在共享接口、顺序性、集成成本风险；
- `DIVERSE_EXPLORATION`：高不确定性或失败处置明确要求探索，且仍有未在同一 task revision + baseline 上完成的 hypothesis。

每次选择连同 run version、integration head、task IDs、资源快照、assessment/contract 和 rationale 一起持久化。策略不宣称求得全局最优 Agent 数量；它的保证是可解释、fail-closed，并且额外 Agent 必须先证明自己值得启动。

#### Contribution 2 — Reliable Coordination

Planner 除 Task DAG 外必须输出轻量 `CoordinationContract / TaskInterface`：

- `provides`
- `requires`
- `assumptions`
- `ownedScope`
- `interfaces`
- `evidenceRefs`

这些字段进入 authoritative store 和每个 Implementer 的证据包；聊天、handoff 和 Agent 的自然语言承诺仍只是 observation。共享 interface、provides/requires 的交叉依赖会阻止并行。Exploration 使用 task revision、baseline commit、investigation key 和 hypothesis 做唯一键，避免在同一事实基线上重复花费探索计算。Task revision 会使旧 coordination contract 失效；Candidate scope、integration head 和 evidence tree 的既有 fencing 继续处理物理写入与 stale result。

#### Contribution 3 — Verified Autonomy

失败不再只有 retry/block 二选一。`FailurePolicy` 按 phase、classification、归一化 detail 生成 fingerprint，并持久化选择：

- `RETRY`
- `REPLAN`
- `DELEGATE`
- `DIVERSE_EXPLORE`
- `REBASE_REVERIFY`
- `INFRA_RETRY`
- `ESCALATE`
- `BLOCK`

相同 deterministic failure 超过阈值后不能继续消耗 Attempt；verification infrastructure error 先在相同 tree/worktree 上原地重试，不立即浪费新的 coding Attempt。产品语义、权限扩大、不可逆操作或无法由 repository/spec/evidence 唯一决定的语义合同，进入 first-class `DecisionRequest / Decision`；旧或非活动 Attempt 无权新建请求。CLI、daemon API 和 Agent control adapter 均有真实通路，选择可释放 task 并继续 run。

最终接受也已 fail-closed：

- `markCandidateEligible` 自己读取 frozen candidate checks；
- `acceptTask` 自己读取 frozen integration checks 与 review requirement；
- `completeRun` 自己读取 frozen run checks；
- 每项 check 必须匹配 subject、exact tree、check kind、规范 hash 和 evidence class；
- caller 已无 `checkIds/reviewIds/requireReview` 参数可用于降低门槛；
- 只有最终树存在通过的 `BEHAVIORAL` 或 `EXTERNAL` run evidence 才是 `VERIFIED_DELIVERY`；仅结构/构建证据的成功终态是 `STRUCTURAL_HANDOFF`；未完成是 `BLOCKED` 或 `CANCELLED`。

### 17.3 与 Claude Code Agent Teams / Pi 的真实差异

| 系统 | 优化对象 | 本项目借用 | 本项目额外负责 |
|---|---|---|---|
| Claude Code / Agent Teams | 强交互式 coding UX、任务委派、teammate 通信与并行 | 独立 context、按需 teammate、任务可见性、P2P 消息、发现后重规划、必要时找人 | 不把 shared task list、Lead 或 teammate 的 Done 当最终 truth；增加 durable epoch、contract、failure disposition、exact-tree acceptance 与 crash semantics |
| Pi | Agent runtime：loop、provider、tools、session、history、context、compaction、retry、RPC/events | 全部直接复用；并从 runtime events 采集 usage | 不修改 Pi 内部 loop；在外部绑定 Task/Attempt/Execution、worktree、Candidate、coordination 和 delivery semantics |
| 本项目 | 多执行体长任务的 verified engineering throughput | 把 Claude 协作语义映射到 Pi execution | 决定何时值得增加 Agent、怎样约束语义承诺、失败后为何换策略、何时必须找人、最终结果为何可信 |

因此回答“为什么不用 Claude Code”时不能说 Claude Code 不会写代码或不可靠。准确回答是：Claude Code 可以是非常强的 worker；本项目研究并实现 worker 之外、不能仅靠 prompt 保证的多执行体控制与交付语义。若任务很小，直接用 Claude Code；若任务跨多轮、并发、崩溃、stale result 和最终验收，本项目接管原本由用户手工承担的 control-plane 工作。

### 17.4 本轮之前已经解决了什么

- Task / Attempt / Execution 分离和 epoch fencing；
- SQLite authoritative truth、append-only domain event、operation journal 与 reconciler；
- Pi official RPC/session/runtime 边界和 crash resume；
- Git input snapshot、每 writer worktree、immutable Candidate、integration CAS；
- candidate/integration/run exact-tree verification、独立 review 与 evidence artifact；
- typed durable/live mailbox 和 bounded task-change proposal；
- local daemon、CLI、取消 fencing 与 terminal report。

### 17.5 本轮新增后真正解决了什么

- “为什么启动额外 Agent”：不再只看 scope，而有可持久化、可解释的结构/语义/成本门控；
- “为什么现在不能并行”：共享 interface、implicit provides/requires、sequentiality、uncertainty 或 integration cost 会强制串行；
- “怎样减少重复探索”：exploration hypothesis 对 task revision + base commit 去重；
- “为什么这次失败要换策略”：failure fingerprint、classification、occurrence 与 disposition 都是系统事实；
- “为什么这里必须找人”：只有窄类型 DecisionRequest 能暂停并等待 authority，普通工程不确定性继续由系统调查；
- “为什么结果可以信任”：Kernel 从冻结契约自行重建验收义务，并按最终树证据强度给出 delivery class；
- “以后怎样证明性能意义”：已经采集 Agent usage、parallel window、check 与 human wait 等原始 telemetry，但未伪造效果结论。

### 17.6 仍然没有解决、也不应夸大的问题

- 没有证明 Agent 数量增加会带来线性或稳定的生产力提升；policy 是保守 heuristic，不是最优调度器。
- Planner 对 semantic coupling 的初始判断仍来自 LLM，并由 repository evidence/rationale 与运行反馈约束，而不是形式化程序分析。
- 没有通用自动 semantic merge；shared interface 会被串行而不是“智能合并”。
- 没有 OS 级 sandbox；工具权限、worktree 和 control capability 是应用层边界。
- 没有跨项目 vector memory、远程 worker、GUI、通用 workflow DSL 或固定角色宇宙。
- 没有完成公开 benchmark 或 fault-injection 数值评测，因此不能声称吞吐、成功率、成本或 SOTA 提升。
- 动态 task proposal 若没有新的完整 coordination assessment，会 fail-closed 地串行执行；系统不编造并行安全性。

### 17.7 中文简历三条（只写源码可证明事实）

1. 设计并实现面向长程仓库级变更的 Agentic Software Engineering Runtime，在 Pi RPC 之上构建 Task/Attempt/Execution 控制平面，通过 SQLite authority、epoch fencing、Pi session/worktree 恢复和 operation reconciliation 支持进程中断后的安全续跑。
2. 实现可解释的 Adaptive Coordination Layer，依据 DAG、可分解性、顺序性、语义/接口耦合、不确定性、集成复验成本和单机资源，在单 Agent、隔离并行、串行和竞争探索之间动态选择；以 CoordinationContract 和 hypothesis 去重降低重复探索与承诺漂移。
3. 构建 Git-native verified delivery：独立 worktree、immutable Candidate、CAS 串行集成、failure fingerprint/disposition、Human-on-Exception，以及由 Kernel 从冻结契约重建的 exact-tree evidence gate，区分 VERIFIED_DELIVERY、STRUCTURAL_HANDOFF 与 BLOCKED。

### 17.8 面试高频追问的冻结回答

**为什么有用？**

长任务的问题不是模型不会生成代码，而是开发者仍需充当人工 control plane。系统把是否并行、失败证据回灌、旧执行 fencing、worktree 集成、最终复验和异常找人做成持久语义，使用户不必持续盯住多个会话。

**为什么难？**

因为额外 Agent 同时增加可用计算和协调成本。路径不冲突不等于语义独立；Agent 的 Done 不等于测试对应最终 tree；进程 crash 后无法只靠 prompt 判断副作用是否发生；stale worker、stale evidence 和 integration race 必须由模型外部系统保证。

**为什么 Multi-Agent？**

Multi-Agent 的唯一意义是扩大可以利用的并行计算预算。系统不追求 Agent 数量：只有真正 dependency-ready、语义独立且集成成本可控时才并行；不满足就用单 Agent 或串行。高不确定性时，少量竞争探索用于缩小错误方向的昂贵写入，而不是长期维持 swarm。

**有什么性能意义？**

目标是提高 verified engineering throughput，而不是单看 token 或 raw solve rate。并行只在预计收益能覆盖 coordination/integration/reverification cost 时开启；系统记录 Agent cost、parallel window、失败/重规划、human wait 和最终 delivery class，以便用同模型同预算实验验证。当前只声称机制和可测性，不声称尚未测出的提升数字。

**为什么不用 Claude Code？**

小任务可以直接使用。当前 TripleTeam 固定通过 Pi 执行；Claude Code 是协作产品语义参考与外部评测基线，没有作为可替换 worker 接入。本项目实现多执行体长任务中的外部 authority、恢复、协调合同、故障处置和最终交付语义。用户是否值得采用，应通过同条件完成率、费用、时间和人工投入验证。

**这是不是 Lead + Workers？**

不是固定 lead team。Planner 只提出 task/coordination plan，Agent 按需实例化；Scheduler、Kernel、Verifier 和 Integrator 是确定性服务。任何 Agent 都不能把自然语言声明升级为系统 truth，也不能降低自己的 acceptance contract。

### 17.9 最终 ADOPT / ADAPT / REJECT

- **ADOPT**：Pi agent loop、provider/tool/session/context/history/compaction/runtime retry/RPC/events；Task/Attempt/epoch、worktree/Candidate/CAS、Kernel-owned exact-tree acceptance；四种 adaptive execution mode；CoordinationContract；FailureDiagnosis/Disposition；DecisionRequest；delivery classification；coordination telemetry。
- **ADAPT**：Claude Code 的独立 context、按需 teammate、task/message/replanning/Human-on-Exception 体验；CAID 的 worktree/asynchronous execution；CATS 的 sequential/parallel compute 分配思想；CooperBench 的 expectation/commitment failure 结论；Gas Town/Warren 的 durable control-plane 经验。全部映射到本项目的单机 authoritative semantics。
- **REJECT**：固定常驻 Agent 队伍、自由聊天 swarm、Agent/Lead 自行 ACCEPT、仅凭 path overlap 并行、相同失败 blind retry、自动 semantic merge、跨项目 vector memory、remote worker、GUI、workflow DSL、重新实现 Pi harness，以及任何没有真实实验支撑的性能数字。

至此核心冻结为：

> **Centralized Authoritative Control + Adaptive Isolated Execution + Evidence-Gated Delivery**

后续实现只能修 bug、补 evaluator 或改善策略质量；不得推翻 Task/Attempt/Execution、coordination contract、failure disposition、DecisionRequest、Candidate/evidence identity、Kernel acceptance 和 Pi runtime boundary。

---

## 18. 审查后实施修订（2026-09-22）

本节说明完成代码审查后已经落地的修订，覆盖第 17 节中与当前实现不一致的细节。产品仍是**面向长程软件工程任务的自适应执行与可信交付系统**，以独立 CLI + 本地 daemon 交付，Pi 是固定执行内核。跨模块功能开发、缺陷修复、重构及升级是同一任务类型的不同实例；项目没有缩小为单一迁移工具。

### 18.1 从并行门控到可审计的计算分配

`execution.policy` 支持 SINGLE、FIXED、HEURISTIC 与 ADAPTIVE。SINGLE 从完整目标建立一个 Task，不先购买模型拆图或探索；它允许全局预算内的 writer 重试，保留与其他条件一致的 review、检查、诊断和无进展止损。因此它是强单 writer 对照，不应描述成整个运行只有一个模型会话。

ADAPTIVE 在原有依赖、scope、顺序性、接口关系等硬约束之上，使用 DAG 关键路径、观测耗时/费用、失败次数与检查负载计算边际收益代理。事件驱动调度在任一执行结束后重新选择可兼容任务。估计输入、先验来源、资源快照和动作均持久化；当前估计器是启发式，成功概率代理未经校准，没有最优性或已训练策略的声明。

高语义耦合维持串行。中等耦合只有在具体共享接口的 obligation 及对应 artifact evidence 成立时才可放宽。无关接口的证据不构成解锁依据。多假设探索传入真实 hypothesis，按 Task revision、baseline、调查键与假设去重，并允许失败后的有界重试；过期报告不进入新事实基线。

### 18.2 合同兑现、消费及兼容演进

`provides` 与 `assumptions` 的每个声明必须具备 `artifactPaths + checkNames`。要求的检查必须存在于冻结 integration checks 中，且不能仅为结构检查。`requires` 绑定唯一 producer 并产生依赖。

合同定义版本与产物实现证据是不同身份：证据保存准确 Git commit/tree、路径、blob、mode、指定检查身份及定义版本。consumer Attempt 绑定自己 baseline 上实际成立的证明。ACCEPTED 状态本身不再把自然语言承诺自动标记为已兑现。

修改已发布产物时，先计算 accepted producer 及受影响的传递依赖闭包，在拟集成树上运行全部相关任务的冻结检查。全部通过后才生成新证明、发布 Git head 并完成接受。失败不得移动 Git/SQLite integration head，也不得提前生成成功证明；旧 baseline 上匹配的 immutable proof 继续有效。改变用户要求的语义仍需授权后的新规格，不能凭新增一份 proof 悄悄改变合同。

这是一种基于产物与检查的运行约束，保证范围取决于 oracle 覆盖。它不等价于对任意代码的完整语义证明。

### 18.3 失败改变实际后续工作

- failure fingerprint 使用实际检查身份、命令版本和失败日志，并按 Task 统计重复。
- REPLAN 执行只读诊断；下一次 writer 保留有效 Candidate，携带检查、review 和冲突证据。
- INFRA_RETRY 在相同 tree 的新验证工作树上重试，避免复用被检查污染的目录。
- 最终检查失败可创建有界 repair Task，冻结原目标、scope、检查与失败证据；不能降低验收标准。
- resume 保留原始 Pi session/context identity，并注入新的运行观察，继续处理任务提案与 DecisionRequest。
- `enableFailureAdaptation=false` 限制为有界 RETRY / INFRA_RETRY / BLOCK，关闭 REPLAN、writer 图修订和最终修复，使消融真正改变行为。

### 18.4 共享预算和 Pi 边界

规划、探索、实现、review、重试与恢复共用 execution、token、费用及 deadline 上限。准入预留和事件用量在 SQLite 内维护；中断保留未决费用，无法观察的账单不得记为零。Pi runtime event 与公共 session statistics 用于增量核对，包括可观察的 compaction/cache/tool usage。模型请求在途及账单滞后使 dollar/token 限额具有边界，不能承诺精确账单硬上限。

effective profile、工具和显式 model/provider/reasoning 被绑定并在后续新 Attempt/恢复时核对。未显式选择模型时，Pi 的默认设置仍属于外部环境；正式评测必须显式固定。所有 provider、agent loop、tools、session/history/context、compaction、abort/steer/retry 继续由 vendored Pi 公共能力承担。

### 18.5 验证强度与人类权限

未知检查默认 STRUCTURAL。native 检查即使声明 BEHAVIORAL/EXTERNAL，也不能独自产生 VERIFIED_DELIVERY；该等级需要受保护的 oracle、固定镜像、只读源码容器及最终准确 tree 的通过证据。每项检查使用独立工作树，检查 tree、index、文件、mode 和修改记录；Docker 生命周期和崩溃后的过期容器纳入回收。

冻结检查与内容保护不能保证验收规则充分，oracle、镜像和宿主机仍是信任基础。writer worktree 和工具权限不是 OS 沙箱。正式盲评的模型可见内容、隐藏评测和网络应由评测环境隔离。

`noninteractive` 在权威入口禁止人类回答、人工 steer/message、手动 retry 及用户提案决策；无法获得所需授权时保留 DecisionRequest 并 BLOCKED。交互模式的有效 A/B 决策会在同一 SQLite 事务中记录答案、回灌上下文和解除相应等待；暂停状态变化不会无故使未改变任务图的提案过期。

### 18.6 评测准备与已执行证据

已提供 FeatureBench 的公开题面输入、冻结 manifest、run 映射、权威树 patch 导出、全部试验收集、官方报告核对及费用汇总；SWE-Milestone 适配只消费官方释放的 queue/SRS，保留自己的连续代码与剩余预算。基线及消融配方区分同模型机制研究与 Codex/Claude Code 产品对照。

本轮 `npm run check`、`npm run build`、`doctor` 通过；包含实际 Docker 用例的测试为 **121 passed / 0 failed / 0 skipped**。这些是实现回归与环境检查，未重跑历史付费 Pi E2E。

另进行一题新上下文 subagent 的封存开发检查，由固定官方 FeatureBench evaluator 评分，严格结果为 **0/1 未解决**，F2P **284/294**、P2P **3508/3508**。没有向 solver 提供隐藏反馈或修复后重交；它不属于 TripleTeam 经 Pi 执行的系统成绩，也不能支持任何成本/质量优势。

最新实现清单见 [实施结果](docs/IMPLEMENTATION_STATUS.md)，配置见 [验证说明](docs/VERIFICATION.md)，评测操作见 [评测入口](docs/BENCHMARK_ADAPTERS.md) 与 [基线配方](docs/BASELINE_COMPARISON.md)，当前面试叙事见 [面试材料](docs/INTERVIEW.md)。历史审查和真实运行记录均保留版本边界。

## 19. 发布界面与多模型接入

产品入口增加 TripleTeam 自己的终端工作台，包含 Overview / Tasks / Activity、静态与 JSON 输出、独立的无 API 示例预览。布局与交互由本项目提供，复用 Pi 公共终端渲染原语；不启动 Pi 聊天界面。观察连接使用 Pi 公共 SQLite `openReadOnly`，不修改运行权威状态或接受结果；视图失败不应阻断执行和恢复。

`execution.roles` 按 planner / explorer / implementer / reviewer 覆盖 provider、model、reasoning。重规划使用 planner 设置，恢复使用 implementer 设置；所有新 Attempt 及恢复继续核对冻结 profile。未配置的角色继承全局设置。API 地址、协议和凭据解析继续由 Pi 用户 registry 承担；项目不新建 provider、认证或模型 agent loop。`tripleteam models` 通过公共 ModelRuntime 读取本地模型元数据，`profiles` 展示角色的有效选择。

## 20. 独立规格与验证计算（2026-09-23）

此前单题开发检查揭示了一个具体缺口：writer 与其测试可以共享错误的规格理解。补齐方式是在 writer 之前增加独立规格设计和可执行探针，并保留原有用户验收的权威边界。

- 新增 `verifier` 模型角色与只读 profile，复用现有 PiWorkerLauncher、runMetered、public RPC。规格设计和独立批判使用两个辅助 REVIEW Attempt，分别记录 SPECIFICATION_DESIGN / PROBE_CRITIQUE phase，不新增 agent loop、provider、session 或并发执行器。
- 每个 behavior obligation 引用公开 goal/task/constraints 的准确子串或基线文件及 blob。probe 将真实 setup 与同断言 contrastSetup 分离；只有断言阶段拒绝错误对照才可冻结。独立 reviewer 排查无依据预期、覆盖缺失、伪造实现和空洞断言。
- SQLite migration 9 保存每个 Task revision 的 immutable assurance plan 和评价批次。批次先落 STARTED，再执行全部重复检查。Kernel 在 candidate eligibility、integration CAS 前、Task acceptance 和最终 completeRun 核对准确 tree、subject、command/version、definition hash 和最新完整批次。恢复不能拼接旧成功记录。
- probes 始终是补充 BUILD 证据，不能凭模型生成测试升级 VERIFIED_DELIVERY。默认 adaptive 按任务风险、现有行为验收与观察到的失败分配检查数量；required/off 提供固定强度与消融。旧 run 保持其冻结语义。
- writer 统计改为按 IMPLEMENT/IMPLEMENT_RESUME 的 Attempt 聚合，验证、设计、review、探索单独计费。可靠性使用实际 writer outcome 的收缩估计，跨 Task 转移最多 4 个等效观察；不把重复 failure diagnosis 视为新的失败样本。额外并行的成本包括辅助调用与检查负载，调查动作记录有界价值代理。系数和概率仍未经校准。
- Python/Go/Rust 项目声明的检查纳入默认发现。native check 子进程移除常见敏感环境变量；其证据等级与 OS 隔离边界保持明确。

全部模型调用沿用同一个预算、冻结 profile 和 Pi 公共 API。真实 DeepSeek 配置使用官方可用模型 ID `deepseek-flash`，密钥通过环境引用；仓库与发布包不保存凭据。实现回归与真实运行结果分别记录在 docs/validation，不能用测试通过替代跨产品性能结论。

### 20.1 真实运行暴露的控制缺口

一次官方遮蔽环境中的开发运行，planner 在 87 次只读工具调用后耗尽 1.5M 总 token，尚未产生 task graph。由此补充 planning allocation：默认 250k token、24 次工具调用、120 秒，且不超过已配置 run token 的 20%；规划请求的调查与格式修正共同扣减。70% 时通过 Pi 公共 steer 提醒结束调查，超过分配通过公共 abort 停止。阶段预留只占相应子额度；不把未知在途费用视为已结算。

规划超额，或经过格式修正仍无法给出合法图时，在全局预算仍可用、没有权限决策等待且尚无 Task 的条件下，确定性建立保留原 scope/验收的全目标 Task，记录 PLANNING_FALLBACK。这个动作保留失败轨迹并改变下一份计算的用途。全局预算耗尽、用户意图不明确或权限欠缺不会被降级路径绕过。

另发现隔离镜像缺少 rg/fd 时，Pi 公共搜索工具的下载被受限网络拒绝，导致模型反复读取。启动前增加调用 createGrepTool/createFindTool 的离线哨兵检查；复用 Pi 工具，不重新实现查找或下载。doctor 与每次 worker 创建都先检查其所需工具，缺失时明确失败。Task 中 NORMAL 与配置 MEDIUM 的历史标签差异也已在新配置加载时统一，旧冻结策略不隐式重写。

模型随后曾把 grep 范围设为 `/`，ripgrep 扫到设备文件并等待到 worker 超时。通过 Pi 公开 `tool_call` hook 对 grep/find 的目标路径与 realpath 做工作树范围检查，所有角色均装载该扩展；有控制能力的 writer 才另外得到协调工具。真实 API 探针确认根目录调用被拦截而本地搜索正常。该范围检查不是 OS sandbox，也不改变 Pi 工具实现。

规格设计增加单次 token/tool/time allocation，保存完整有界的失败定义和具体原因，重启后继续局部纠正；精确原文引用门禁保持不变。设计额度耗尽属于验证阶段的可解释阻塞，不能冒充基础设施故障重复启动 writer。首次开发试跑说明默认额度仍需按仓库复杂度配置，不能将静态阈值描述为已学会的最优预算策略。

随后将验证设计的顺序调整为来源/结构校验、实际错误对照、独立模型批判、冻结。错误对照发生 NameError/AttributeError 或无法区分错误实现时，先局部修订，不购买 reviewer 调用；对照通过也不能跳过独立批判。这是依据真实失败成本调整计算顺序，不改变验收权威。

阶段中断还暴露了公共 RpcClient.promptAndWait 的事件等待会在 stop 后保留超时器。适配器改用公开 prompt/onEvent 组合管理一次执行的等待，在 abort、stop、timeout、传输失败时清理监听器和计时器；保留 Pi 的 agent loop、取消和 session statistics，不修改 vendor 或调用私有实现。独立子进程回归确认长等待取消后可以及时退出，取消不能被记为成功。

真实交互执行又暴露了协议纠错的细节：模型将 FILE.path 写成 source 时，笼统的错误不足以支持局部修复。来源错误现包含义务/来源索引、实际字段与合法例子；JSON 语法失败保存有界原始输出和截断标记，不能抹掉上一份完整定义。这里改进反馈质量，保持来源校验和有限重试，不自动猜测字段或放宽冻结条件。

### 20.2 交互入口与冻结设置

默认 CLI 改为产品自己的单列目标输入与状态界面，复用 Pi 公开终端输入/渲染原语。斜杠命令直接调用同一控制面，状态读取、设置修改与执行可并存；调度、acceptance 和 agent loop 不进入 UI。

项目设置通过完整元数据分组展示，类型校验后原子写入；role model 可同时设置 provider/model。新 GoalContract 除配置 hash 外保存完整有效配置。initialize 读取新文件而不改活跃服务，run/continue 在独占执行前切回该 run 的冻结配置；旧的仅 hash 记录继续要求原配置。修改 UI 设置不会改写运行中的验收、权限或预算。

### 20.3 本轮验证与正式测评边界

最终实现回归为 277 passed / 0 failed / 0 skipped，包含固定 Docker 镜像的两个用例。最新默认交互入口通过实际 Pi + DeepSeek 执行受控任务，24 项检查通过并形成 VERIFIED_DELIVERY；先前失败的交互试跑保留。真实 PTY 覆盖中文、缩放、控制命令和信号退出码，详见 [发布记录](docs/validation/2026-09-23-release.md)。

官方遮蔽环境中的 FeatureBench 五次开发试跑均未完成交付。最后从权威集成树封存的空补丁由官方 evaluator 判未解决，不能选择未接受的 writer Candidate 代替交付。记录了预算/工具配置变化，以及中间集成公开测试的导入收集错误；这些开发配置不是正式公平比较。后续需要先在准确基线工作树验证环境，再冻结适合阶段产物的检查与整体最终验收，不能在运行失败后删掉冻结检查。所有失败与费用边界见 [开发记录](docs/validation/2026-09-23-featurebench-development.md)。

## 21. 可验证增量与剩余交付计算（2026-09-23）

本节记录本项目新增的决策机制，不声称复现 Claude Code 内部实现。Pi 的 loop、session、context、tools、provider 与 retry 仍通过固定版本公开接口使用。

### 21.1 验收粒度是规划的输入

CheckCommand 增加 scope、atomic 与固定的 native preparation。启动时在准确 input tree 的干净 worktree 上执行冻结 integration checks；baseline 记录使用独立 checkKind，不能充当最终检查。普通失败提供“当前增量无法独立验收”的观察；基础环境/完整性错误先阻止模型计算。continue 可重新检查之前不可用的环境。

Planner 提案经确定性 increment compiler 编译：atomic 检查覆盖多个任务时取并集，合并造成的依赖环通过吸收中间节点消除。保留各任务目标、约束、风险、接口义务及外部依赖；同 key 的义务合并其全部 artifact/check 要求。全局基线失败时直接保留完整目标。scope 选择不能移除全局检查，Kernel 在创建任务和接受改图时强制核对版本与 review 条件。

这解决的是“怎样形成可以继续集成的进展单位”，不是用放宽验收换通过率。最终 runChecks 始终绑定冻结合同和最终 Git tree。

### 21.2 可选验证的计算价值

新 run 固定 verificationPolicyVersion=1。对每个 task revision 记录一次 VERIFICATION_ALLOCATION：用已完成 writer 的时间/费用、失败观察、已有行为检查及剩余资源，估算额外检查设计/批判能避免多少返工，并为剩余 writer、必需 review 和终验预留估算空间。required 或 HIGH 风险继续需要独立验证。后续预算变化不能撤销已冻结的探针。

这是未校准、可消融的 value-of-information 启发式。调度、验证分配、失败处置分别由现有策略承担，没有把它包装成已学习或最优的统一价值函数。

### 21.3 观察复用与语义上下文

通过 Pi RpcClient 的公开 tool_execution_start 事件记录 read/grep/find/ls 的文件或目录依赖；观察包含 Git blob/tree、文件 mode 和缺失文件。公开 DefaultResourceLoader 提供用户/祖先上下文、skills 和 prompt 版本，仓库 .pi、AGENTS/CLAUDE 等上下文及忽略规则也参与有效性检查。越出工作树、读取 symlink 或使用未知工具的调查不进入复用。

只有问题、假设、反馈、任务 revision、profile、上下文及读取依赖均相符时才省掉一次 explorer 调用，并记录原 Attempt 与重新验证的 baseline。目录搜索包含整个目录 tree，新增文件会使观察失效。复用对象是调查结论，不能代替准确树上的 Check/Review/Acceptance。

### 21.4 协议与产品验收

消费者的输入包现在包含已在其 baseline 上验证的上游 obligation、blob、mode、check ID 和版本。独立 critique 的不完整 JSON 可在同一 Pi session 内用原阶段剩余额度纠正一次；不重新设计已验证的 probes，不把无效响应当作批准。Pi provider 的 error/aborted 结束也不能被包装成成功模型响应。

CLI `/why` 展示实际调度与验收缺口，`demo create` 生成跨 contracts/jobs/API/SDK 的真实公开任务，演示脚本通过 HTTP 跑通交付代码。正式性能由使用者运行 benchmark；固定配置、无人工入口、完整失败账本和配对分析见 [验收](docs/ACCEPTANCE.md) 与 [评测指南](docs/BENCHMARK_GUIDE.zh-CN.md)。

### 21.5 独立探针的阶段语义

真实 Atlas 开发试跑发现：仅允许修改 contracts 的前置任务，被模型设计成要通过尚未实现的 API/worker 行为。这不是应靠重试或扩大 writer scope 解决的失败。新增探针 stage（TASK / FINAL）：TASK 在候选与集成阶段执行，FINAL 在全部下游完成后执行；最终树重跑二者。至少一个 TASK 探针、来源绑定、对照控制、独立 critique 和冻结定义仍是必要条件。旧定义省略 stage 时维持逐阶段执行，绝不改变已有冻结计划。运行器与 Kernel 使用同一个纯选择函数，但 Kernel 仍独立核验 tree、subject、batch、command/version 和定义 hash。设计上下文包括 scope、接口合同、已有增量检查和其他任务；只选择少量准确表述的补充义务，避免把全项目规格重复强加给每个局部任务。对应真实 Git 回归同时检查“局部可过、终验不能跳过下游、最后完整通过”和非法全 FINAL 计划拒绝。

### 21.6 供应商永久错误与运行级停止

Atlas 第四次开发试跑的 Pi 公共 assistant errorMessage 明确为 HTTP 402 Insufficient Balance。错误原先被压缩成通用 runtime error，导致同一账户的后续 writer 被无效重试。薄适配器现在只把明确的余额/额度、认证、权限错误映射为不携带原始响应体的分类错误；普通 429/5xx 不被误标成永久错误。runMetered 落盘 PROVIDER_UNAVAILABLE，Kernel 的预约、流式检查与调度快照一起停止新增计算，其他在途调用走既有 Pi abort。显式 resumeRun 记录恢复请求，允许在已修复外部账户后再试，原有预算、时间期限和验收条件全部保留。不新增 provider SDK、重试环或凭据管理。
