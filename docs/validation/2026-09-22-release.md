# Source release validation — 2026-09-22

This snapshot includes the independent TripleTeam terminal interface, per-role model selection, English and Chinese product documentation, and source packaging.

## Executed checks

| Check | Result |
| --- | --- |
| `npm run check` | Passed: TypeScript and Biome |
| `npm run build` | Passed: CLI, daemon, control extension and benchmark entry points |
| Full test suite with the pinned Docker test image | **142 passed, 0 failed, 0 skipped** |
| `npm run preview:assets` | Passed: terminal illustration generated from the actual dashboard renderer |
| Local Markdown links, example JSON and SVG XML | Valid |
| `git diff --check` | Passed |

The Docker test image was `sha256:7ce4b6dfe35e55397b7cda544f8a13f191b7ae28dc5aad71fe664dbc9bc2623f`. Set `TRIPLETEAM_TEST_DOCKER_IMAGE` to that available local image to reproduce the full test run; without a configured image, two optional Docker tests are skipped.

The 21 added presentation and model-routing tests cover plain/JSON output, terminal width and control-character handling, read-only dashboard access, terminal cleanup, fallback behavior, role inheritance, frozen profile selections, resume and all six execution paths. Provider configuration is passed through the existing Pi boundary. The release checks make no model API calls and do not measure model quality, provider availability, cost savings or benchmark performance.

Source archives include a per-file SHA-256 manifest. Archive checksums are written beside the archives in `release/SHA256SUMS`. Existing implementation and evaluation records remain in [implementation status](../IMPLEMENTATION_STATUS.md).
