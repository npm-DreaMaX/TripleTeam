# Source release validation — 2026-09-23

This source snapshot includes the independent interactive CLI, per-role model/API selection, editable project settings with frozen run configuration, independent specification probes, bounded planning and design allocations, recovery fixes, and the associated documentation.

## Executed checks

| Check | Result |
| --- | --- |
| `npm run check` | Passed: TypeScript and Biome |
| `npm run build` | Passed: CLI, daemon, control extension and benchmark entry points |
| Full suite with pinned Docker image | **277 passed, 0 failed, 0 skipped** |
| `node dist/cli.js doctor --json` | Passed: Node, Git, vendored Pi, actual search tools and SQLite |
| `node dist/benchmark.js --help` | Passed |
| Real terminal signals and restoration | Ctrl+C/SIGINT 130, SIGTERM 143, normal quit 0 |
| Model templates and documentation | 50 local file links and three model/project templates checked offline |
| Wide and narrow terminal previews | Rendered from product components and visually inspected |
| `git diff --check` | Passed |

The complete test invocation was:

```bash
npm run check
TRIPLETEAM_TEST_DOCKER_IMAGE=sha256:7ce4b6dfe35e55397b7cda544f8a13f191b7ae28dc5aad71fe664dbc9bc2623f npm test
npm run build
node dist/cli.js doctor --json
```

The image identity is local and fixed. Without an available configured image, the suite skips two optional Docker cases. The result above includes both. Tests cover real Git operations, SQLite transactions, executable probe controls, crash recovery, RPC cancellation cleanup, shared/stage budgets, frozen configuration, source diagnostics, independent review gates and terminal lifecycle. Deterministic worker fixtures remain distinct from model-quality measurements.

## Live execution evidence

- [Two single-goal API runs](2026-09-23-controlled-runtime.md) reached `VERIFIED_DELIVERY`.
- [A two-module API run](2026-09-23-two-module-runtime.md) caught an implementation defect with independent probes, retried and reached `VERIFIED_DELIVERY`. Its writer inference windows did not overlap.
- [Two real terminal API trials](2026-09-23-terminal-runtime.md) retain an initial design-protocol failure and the subsequent verified delivery through the latest CLI.
- [FeatureBench development trials](2026-09-23-featurebench-development.md) retain all five unsuccessful trials, configuration changes, usage uncertainty and the independently graded sealed submission.

These are implementation and development records. Relative accuracy, cost, latency and human-effort advantages over Codex or Claude Code require a separately frozen comparison.

The source archives contain the current working tree, built entry points, fixed vendored Pi snapshot and a per-file SHA-256 manifest. They contain no Git history, installed node_modules, runtime state or API credentials. Archive hashes are written to `release/SHA256SUMS`; publishing instructions preserve the destination Git history in [PUBLISH.md](../PUBLISH.md).
